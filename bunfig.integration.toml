[test]
# Integration configuration. Migrated suites under `test/infrastructure/integration/`.
root = "./test/infrastructure/integration"
timeout = 60000

[install.scopes]
"@octopus-synapse" = { token = "$BUN_AUTH_TOKEN", url = "https://npm.pkg.github.com" }
