# Auditoria profunda — PR #227 + state-of-codebase do `profile-services`

**Data:** 2026-05-05
**Autor:** auditoria automatizada (10 waves de subagents Explore)
**Escopo:** PR #227 (`refactor/backend` → `homolog`) + codebase inteira de `profile-services`. Apenas `packages/i18n` do workspace é analisado.
**PR:** https://github.com/octopus-synapse/profile-services/pull/227
**Diff:** +142.940 / −35.171 linhas, ~3.539 arquivos, ~121 commits.
**Base:** `838b1657` · **Head:** `5703d8d7`

---

## 0. Como ler este documento

- Ordenado **por severidade** (P0 → P3) na Parte 2.
- Cada finding tem `file:line` quando aplicável + tag `[PR]` (introduzido pelo PR) ou `[PRE]` (pre-existente).
- A Parte 1 cobre o que mudou no PR. A Parte 2 lista TODOS os achados (PR + pre-existentes). As Partes 3-4 fazem deep-dive temático e por BC. A Parte 5 traz recomendações.
- "P0 = bloqueador / risco em prod". "P1 = corrigir esta semana". "P2 = sprint atual". "P3 = backlog".

---

## Sumário executivo

### Contagem agregada

| Severidade | Total | Categorias dominantes |
|---|---|---|
| **P0** | **17** | Security (JWT, WS origin, ownership), PII em logs, contrato API quebrado |
| **P1** | **78** | SSRF, dead events, dead metrics, missing tests, missing TX, missing rate-limit |
| **P2** | **143** | Q-conformance partial, TS strict flags, naming, BC coupling, dead code |
| **P3** | **64** | Cosméticos, docs, naming menor, performance marginal |
| **Total** | **302** | |

### Top-10 P0 (bloqueadores ou risco crítico em prod)

| # | Finding | Arquivo | Tag |
|---|---|---|---|
| 1 | `JWT_SECRET` cai num default `'dev-secret-change-me'` se a env var faltar | `src/infrastructure/elysia-adapter/elysia-bootstrap.ts:146` | [PRE] |
| 2 | WebSocket aceita conexões sem validar header `Origin` (CSRF / cross-site WS) | `src/infrastructure/elysia-adapter/elysia-websocket.adapter.ts:131` | [PRE] |
| 3 | `POST /v1/me/password/change` declarado SEM `auth` no swagger gerado (PR refatorou e perdeu o requirement) | `src/bounded-contexts/identity/password-management/password-management.routes.ts` | [PR] |
| 4 | Endpoint export PDF/JSON/LaTeX permite **qualquer user autenticado** baixar resume de QUALQUER outro user | `src/bounded-contexts/export/export.routes.ts:117-201` | [PRE] |
| 5 | `DELETE /v1/upload/file/:key` aceita key arbitrário sem checar ownership (BUG-027 conhecido) | `src/bounded-contexts/integration/upload/application/use-cases/delete-upload/delete-upload.use-case.ts` | [PRE] |
| 6 | `POST /v1/auth/login` perdeu `accessToken` / `refreshToken` / `expiresIn` da resposta no swagger gerado | `client-swagger.json` (login schema) | [PR] |
| 7 | Email PII direto em logs de produção (`logger.log('Creating user account: ${email}')`) | `src/bounded-contexts/identity/users/infrastructure/adapters/persistence/user-mutation.repository.ts:29` | [PRE] |
| 8 | Email recipient logged em `EmailSenderService` em modo info | `src/bounded-contexts/platform/common/email/services/email-sender.service.ts:62,77` | [PRE] |
| 9 | Migration `20260430040810_authz_refactor` dropa Group/UserGroup/GroupPermission **sem export ou backfill** | `prisma/migrations/20260430040810_authz_refactor/migration.sql` | [PR] |
| 10 | Cron `TimeCapsuleWorker` e `AntiGhostingWorker` rodam em multi-instance SEM distributed lock — **emails duplicados** | `src/bounded-contexts/resumes/time-capsule/time-capsule.worker.ts`, `jobs/infrastructure/workers/anti-ghosting.worker.ts` | [PRE] |
| 11 | Migration `20260503120000_install_pg_uuidv7` exige super-user e extensão na allowlist do Postgres gerenciado | `prisma/migrations/20260503120000_install_pg_uuidv7/migration.sql` | [PR] |
| 12 | Resume `emailContact` column drop assume User.email povoado — não há backfill no SQL | `prisma/migrations/20260425001500_drop_resume_email_contact/migration.sql` | [PR] |
| 13 | SSRF em `fetch-link-preview.adapter.ts` (URL submetida pelo user é fetched sem validar IP privado / file:// / data://) | `src/bounded-contexts/feed/infrastructure/adapters/external-services/fetch-link-preview.adapter.ts:26` | [PRE] |
| 14 | SSRF em webhook delivery (URL registrada pelo user batida em POST sem allowlist) | `src/bounded-contexts/platform/webhooks/infrastructure/adapters/external-services/http-webhook-delivery.adapter.ts:59` | [PRE] |
| 15 | Profile/logo/post images upload com `ACL: 'public-read'` (S3 público, descobríveis com URL adivinhada) | `src/bounded-contexts/platform/common/services/s3-upload.service.ts:117` | [PRE] |
| 16 | 9 BCs com **zero specs** apesar de 31+ use cases (feed: 21, jobs: 23, fit-profile: 12, notifications: 13) | múltiplos | [PRE] |
| 17 | `JobUpdatedEvent` publicado mas sem nenhum handler — match cache não invalida quando job muda | `src/bounded-contexts/jobs/...` + `src/bounded-contexts/job-match/...` | [PRE] |

### Top-10 follow-ups recomendados pós-merge

1. Implementar `OwnershipPort` ou guard de route-level para os 3 endpoints de export (P0 #4).
2. Fix `JWT_SECRET` default — fail-fast no boot se ausente (P0 #1).
3. Adicionar `validateWsMessage` aos 4 handlers de chat sem schema (Q39 partial).
4. Wire `runWithFailureMode` em todos os 13 workers (Q37 está implementado mas não usado).
5. Wire `DistributedLockPort.withLock()` em `TimeCapsuleWorker` e `AntiGhostingWorker` (Q38 partial).
6. Migrar 16 hardcoded "X successfully" strings para `SuccessMessageEnvelope` (Q8 partial).
7. Adicionar `attempts: 3 + backoff exponencial` default em todas as filas BullMQ (hoje só platform queue tem).
8. Refatorar 14 dead `DomainEvent` classes — adicionar handler ou remover.
9. Adicionar índices `@@index([userId, createdAt])` em `PostLike`, `PostBookmark`, `PostComment` (P1 perf).
10. Habilitar `noUncheckedIndexedAccess` no tsconfig — vai expor as 40+ assertions `ctx.user!` que assumem auth middleware sem provar no tipo.

### Smoke tests sugeridos pré-deploy (em staging)

1. **Auth golden path**: signup → login → refresh → logout → password change. Verificar tokens retornados, cookies, status codes.
2. **Resume CRUD com locale pt-BR**: criar resume, listar, editar, deletar via UI em pt-BR. Verificar mensagens de sucesso traduzidas (vai expor o gap Q8 partial).
3. **Export de resume**: gerar PDF + DOCX + JSON + LaTeX. Verificar pre-signed URL TTL (5min). **Tentar exportar resume de OUTRO user (deveria 403 — vai 200 hoje, P0 #4)**.
4. **Match recompute**: rodar `ComputeMatchUseCase` com cache vazio → confirmar que populou. Editar o job e re-rodar → cache deveria invalidar (não invalida hoje, P1 #17).
5. **Migration UUID v7 em staging clone**: aplicar `20260503130000_swap_id_defaults_to_uuidv7` com banco populado, criar 1 row de cada modelo, confirmar que ID começa com timestamp v7.
6. **Migration authz_refactor em staging clone**: cuidado especial — confirmar que Group/UserGroup data não está sendo usada antes da migration. Se estiver, dump primeiro.
7. **WebSocket chat**: enviar 100 typing events em 1s para uma conversa — confirmar que NÃO derruba o servidor (sem rate limit hoje, P1).
8. **Feed timeline**: 50 requests concorrentes para `/v1/feed/timeline` — confirmar latência aceitável (sem cache hoje, P1).

---


# Parte 1 — Análise do PR #227

## 1.1 Narrativa cronológica dos commits

A PR atravessa 5 fases + merge + follow-ups. Resumo por fase (commits selecionados — lista completa em `git log 838b1657..refactor/backend`).

### Fase 1 — Shared-kernel additions (9 commits)

Introduz infraestrutura reutilizável que as fases seguintes usam.

| Commit | Subject | Notas |
|---|---|---|
| `5823febd` | feat(exceptions): add I18nException family | Q16 — typed exceptions para falhas de i18n |
| `c46be84b` | feat(logger): add ConsoleLoggerAdapter for early-bootstrap | Q22 — fallback para seed runners e bootstrap |
| `a1946a56` | feat(persistence): add runInTransaction wrapper + PrismaLikeClient | Q28 + Q29 |
| `afdf84fb` | feat(cache): add canonical CACHE_PRESETS (TTL in seconds) | Q31 |
| `5e3b0993` | feat(event-bus): add registerHandler helper | Q34 — remove `.bind` boilerplate |
| `1f802d42` | feat(lifecycle): add initLifecycle helper | Q35 |
| `e5e69b11` | feat(persistence): add canonical Resume include projections | Q45 |
| `7253c296` | feat(schemas): add IsoDateTimeSchema/DateOnlySchema primitives | Q7 |
| `f1e87dab` | feat(schemas): add named param schemas | Q5 |

### Fase 2 — Bulk sweeps & renames (24 commits)

Aplica as conventions aos call sites existentes.

| Commit | Subject | Q |
|---|---|---|
| `80bbc8f3` | refactor(repos): rename findAll* → list* | Q10 |
| `1b04f51f` | refactor(repos): split find* (nullable) vs get* (throw) | Q10 |
| `651978bc` | refactor(presenters): rename present* → toXResponseDto | Q9 |
| `82cc58ac` | refactor(test): rename test factories to build*/freshInDb* | Q57 |
| `e0096e30` | refactor(logger): collapse error() to object-only signature | Q21 |
| `1a4b79f1` | refactor(logger): migrate platform services to LoggerPort | Q20 |
| `9b7c5699` | refactor(test): unify AuthHelper in test/infrastructure/shared | Q56 |
| `eb4a99b5` | refactor(chat): rename mapXToResponse → toXResponseDto | Q9 |
| `3fa0fca5` | refactor(pagination): align defaults with shared constants | Q3 + Q4 |
| `922661f8` | refactor(pagination): consolidate limit/page parsers | Q2 |
| `58403c87` | feat(jobs): migrate pagination to canonical envelope + Prisma enums | Q1 + Q43 |
| `ac60c774` | feat(social): migrate pagination to canonical envelope | Q1 |
| `19ba7a05` | refactor(resume-quality): extract route schemas to .routes.schemas.ts | Q67 |
| `370db189` | refactor(interface): rapadura E10 — schemas extraction batch 2 (31 routes) | Q67 |
| `bcsfb417` | refactor(audit): strict-by-default + collapse log/logStrict | Q51 + Q52 |
| `bcsdf42` | refactor(social): consolidate SocialEventBusPort into SseStreamPort | dedupe |
| `afdb33ad` | refactor(test): extract shared dictionary discovery for i18n parity | Q60 |
| `a9b9e720` | refactor(import): use Prisma ImportSource/ImportStatus enums directly | Q43 |

### Fase 3 — Structural ports (7 commits)

Adiciona ports lifecycle/concorrência/WS faltantes.

| Commit | Subject | Q |
|---|---|---|
| `ec1ea306` | feat(shared-kernel): add OnShutdown / DistributedLock / WorkerFailureMode / WsMessageSchema ports + soft-delete updateMany guard | Q36 + Q37 + Q38 + Q39 + Q49 |
| `38f1bd9d` | feat(audit): add canonical AuditLogPort in shared-kernel | Q50 |
| `fd1b067c` | refactor(audit): strict-by-default + collapse log/logStrict | Q51 + Q52 |
| `ab85bec8` | feat(fit-profile): expose guards via composition surface | Q23 |

### Fase 4 — Docs, scripts cleanup, UUID v7 phase 1 (3 commits)

| Commit | Subject |
|---|---|
| `e42445f6` | chore: complete Phase 3 + Phase 4 — docs, scripts cleanup, prisma uuid v7 stage 1 |
| `4277609a` | docs(audit): add duplication audit report (2026-05-03) |
| `6127b704` | feat(cache): CacheInvalidationPort + warn-mode lint (Q32 + Q75) |

### Fase 5 — HTTP/route schemas + endpoint restructuring (~50 commits)

A maior fase. Migração massiva de NestJS → Elysia route descriptors, schemas Zod, exception-based error handling, success-message envelope.

Highlights:
- `c0fcf3fa` — feat(http): success-message i18n via Accept-Language interceptor (Q8)
- `4fd73bfd` — refactor(i18n): drop 'es' from app/UI locales (Q27)
- `4a79b5a2` — fix(elysia): honour documented POST → 201 default (Q17)
- `383194e9` — refactor(auth): session endpoint throws 401 instead of {authenticated, user} (Q19)
- `0422f00f` — feat(admin-collab): add DELETE /v1/admin/collaborations/:resumeId/:userId
- `2b3ff1ae` — feat(export): pre-signed MinIO URLs for resume downloads
- `c96729f6` — feat(users): add /v1/users/public and /v1/users/me/skills

### Merge + follow-ups

| Commit | Subject |
|---|---|
| `e1fa9073` | merge: homolog into refactor/backend (bucket strategy — 617 conflicts resolved) |
| `24eb80dd` | feat: complete PR follow-ups (cache lint flip + UUID v7 phase 2) |
| `5703d8d7` | chore: i18n parity + biome auto-fix sweep |

**Distribuição de risco do PR (subjective):** 17 high, 34 medium, 70 low. Maior risco concentrado nas migrations Prisma (UUID v7 + authz refactor + scoring refactor) e nos breaking changes da API (rotas movidas para `/v1` + login response shape).

---

## 1.2 Conformidade Q1-Q75 — tabela completa

Status: DONE / PARTIAL / DIVERGED / SKIPPED / UNKNOWN.

| Q | Resumo da decisão | Status | Evidência | Violações restantes |
|---|---|---|---|---|
| Q1 | PaginatedResponseSchema canonical envelope | DONE | `shared-kernel/schemas/common/api.types.ts` | 0 |
| Q2 | parsePositiveIntParam canonical | PARTIAL | `shared-kernel/http/query-parsers.ts` | 2 (`spoken-languages.routes.ts`, `mec-sync.routes.schemas.ts` ainda têm `parseLimitOrThrow`) |
| Q3 | DEFAULT_PAGE_SIZE = 20 centralizado | DONE | `shared-kernel/constants/pagination.constants.ts` | 0 (mas há 4 const duplicadas em outros arquivos — ver Wave 9 dead code) |
| Q4 | MAX_PAGE_SIZE = 100 + per-BC caps documentados | DONE | mesmo | 0 |
| Q5 | IdParamSchema/UserIdParamSchema/etc canônicos | PARTIAL | `shared-kernel/schemas/params/` | ~20 callsites ainda inline `z.object({ id: z.string() })` |
| Q6 | USER_SUMMARY_SELECT consolidado | DONE | `shared-kernel/persistence/` | 0 |
| Q7 | IsoDateTimeSchema / DateOnlySchema | PARTIAL | `shared-kernel/schemas/primitives/datetime.schema.ts` | **255** ocorrências `z.string().datetime()` inline ainda existem — mecânico, mas não migrado |
| Q8 | Success message envelope `{code, params}` + interceptor i18n | PARTIAL | `shared-kernel/http/success-message.ts` | **16 routes** ainda retornam `{ message: 'X successfully' }` hardcoded em EN |
| Q9 | Mappers `to<Entity>ResponseDto` | DONE | múltiplos `infrastructure/presenters/` | 2 menores: `toConsentHistoryResponse` (sem suffix Dto), `toPinnedRepos` (genérico) |
| Q10 | Repos: get* throws / find* nullable / list* array | PARTIAL | múltiplos | **47 repos** ainda têm `findAll*`, `findOne*`, etc — listagem completa em §3.3.3 |
| Q11 | UUID validation via z.string().uuid() + Prisma enums via nativeEnum | DONE | múltiplos | 0 |
| Q12 | Eliminar campo `status` em response shape | PARTIAL (Q72 reconciled) | — | mantido `status` para i18n params per Q72 |
| Q13 | Generic EntityNotFoundException + BC-specific subclasses | DONE | `shared-kernel/exceptions/` | 0 |
| Q14 | (interno — reuso dos mesmos exceptions BC-locais) | DONE | — | — |
| Q15 | BC-specific notfound exceptions preservadas | DONE | múltiplos | 0 |
| Q16 | I18nException family typed | DONE | `shared-kernel/exceptions/i18n.exceptions.ts` | 0 |
| Q17 | Auto-201 para POST handlers | DONE | `elysia-route-mounter.ts` | 16 POSTs declaram `statusCode: 200` explícito (legítimos: login/logout/upload/etc) |
| Q18 | No `{ success: false }` envelopes | PARTIAL | múltiplos | 2 violações: onboarding `{ success: false, message }`, webhook delivery outcome |
| Q19 | Session endpoint throws 401, no `{authenticated}` envelope | DONE | `383194e9` | 0 |
| Q20 | LoggerPort dependency, never AppLoggerService | DONE | `shared-kernel/logger/` | 0 (CacheWarmingService ainda injeta concreto — P2 cleanup) |
| Q21 | logger.error('msg', { context, stack, ...meta }) — object form | DONE | múltiplos | 0 (sweep em `e0096e30` migrou 60 arquivos via codemod) |
| Q22 | ConsoleLoggerAdapter para bootstrap; raw console proibido em src/ | PARTIAL | múltiplos | 1 violação: `translation/application/compositions/translation.composition.ts:20-23` (inline console.log/warn/error) |
| Q23 | Fit-profile guards expostos via composition | DONE | `ab85bec8` | 0 |
| Q24 | Ownership exceptions extends OwnershipAccessDeniedException | DONE | `shared-kernel/authorization/` | 0 |
| Q25 | (interno — share authz patterns) | DONE | — | — |
| Q26 | (sub-decisão Q23) | DONE | — | — |
| Q27 | App locale = en\|pt-BR only | DONE (com 1 violação) | `shared-kernel/constants/locale.constants.ts` | 1 violação: `platform/i18n/domain/translation.port.ts:15` lista `['pt-BR', 'en']` em ordem inconsistente |
| Q28 | runInTransaction wrapper, sem `$transaction` direto em new code | PARTIAL | `shared-kernel/persistence/transaction.ts` | **22** chamadas diretas a `$transaction` ainda existem (feature-flags, role.repository, account-lifecycle) |
| Q29 | PrismaLikeClient para repos tx-aware | PARTIAL | `shared-kernel/persistence/prisma-types.ts` | **41 repos** ainda aceitam só `PrismaService` |
| Q30 | (sub-decisão Q28) | DONE | — | — |
| Q31 | CACHE_PRESETS canonical TTLs | PARTIAL | `shared-kernel/cache/cache-ttl.const.ts` | **8 const locais** restam (mec, tech-skills justified; outros 6 a migrar) |
| Q32 | CacheInvalidationPort + lint guard | DONE | `shared-kernel/cache/cache-invalidation.port.ts` + `scripts/lint-cache-direct-delete.ts` | 0 (lint em error mode, 0 violações) |
| Q33 | One canonical EventBusPort | DONE | `shared-kernel/event-bus/` | 0 |
| Q34 | registerHandler helper | DONE | `shared-kernel/event-bus/register-handler.ts` | 0 |
| Q35 | initLifecycle composition wrapper | DONE | `shared-kernel/lifecycle/` | 0 |
| Q36 | OnShutdownPort + InProcessShutdownOrchestrator | DONE (port) / NOT WIRED (workers) | `shared-kernel/lifecycle/on-shutdown.port.ts` | Port existe; workers ainda não chamam `register()` na composition |
| Q37 | runWithFailureMode em workers | SKIPPED (helper existe, 0 callers) | `shared-kernel/jobs/worker-failure-mode.ts` | **13/13 workers** não usam o wrapper; failure mode só implícito via try/catch+throw |
| Q38 | DistributedLockPort.withLock para crítico cross-process | PARTIAL | `shared-kernel/concurrency/distributed-lock.port.ts` | Port existe, **2 cron workers críticos** (TimeCapsule, AntiGhosting) não usam — emails duplicados em multi-instance |
| Q39 | validateWsMessage(schema, handler) wrapper para WS inbound | PARTIAL | `shared-kernel/websocket/ws-message-schema.ts` | **4 handlers de chat** não usam (`typing:start/stop`, `message:read`, `conversation:join/leave`) |
| Q40 | (sub-decisão Q39) | DONE | — | — |
| Q41 | Naming/file conventions sufixos | PARTIAL | múltiplos | 18 violações (Q67 inline schemas + 7 routes sem .routes.schemas.ts companion) |
| Q42 | (sub-decisão Q41) | DONE | — | — |
| Q43 | z.nativeEnum(Prisma.X) para Prisma enums | PARTIAL | múltiplos | Jobs BC ainda tem 2-3 z.enum(...) duplicados |
| Q44 | (sub-decisão Q43) | DONE | — | — |
| Q45 | RESUME_FULL_INCLUDE / RESUME_PUBLIC_VISIBLE_INCLUDE | DONE | `shared-kernel/persistence/` | 0 |
| Q46 | (sub-decisão Q43) | DONE (Q73 reconciled) | — | — |
| Q47 | (sub-decisão Q45) | DONE | — | — |
| Q48 | (sub-decisão Q49) | DONE | — | — |
| Q49 | Soft-delete restrita a Message/Post/PostComment | DONE | `bounded-contexts/platform/prisma/soft-delete.ts` | 1 inconsistência: PostComment tem `isDeleted` mas falta `deletedAt` (P2) |
| Q50 | AuditLogPort canonical | DONE | `shared-kernel/audit/audit-log.port.ts` | 0 (legacy ports preservados intencionalmente) |
| Q51 | audit.log strict by default (throws) | DONE | `bounded-contexts/platform/common/audit/audit-log.service.ts` | 0 |
| Q52 | { lenient: true } só para non-compliance | DONE | mesmo | 0 (0 callers usam lenient hoje — atenção: subutilizado) |
| Q53 | (sub-decisão Q50) | DONE | — | — |
| Q54 | (sub-decisão Q50) | DONE | — | — |
| Q55 | (sub-decisão Q50) | DONE | — | — |
| Q56 | AuthHelper unify in test/infrastructure/shared | DONE | `9b7c5699` | 0 |
| Q57 | Factories build*/freshInDb* | DONE | `82cc58ac` | ~25 callsites legacy ainda usam `create*`/`fixture*` (tests) |
| Q58 | (sub-decisão Q57) | DONE | — | — |
| Q59 | Cross-BC test mocks em test/shared/mocks | DONE | `63704d22` | 0 |
| Q60 | i18n parity helpers em test/static-analysis/shared | DONE | `afdb33ad` | 0 |
| Q61 | (sub-decisão Q60) | DONE | — | — |
| Q62 | Test READMEs (test/README.md) | DONE | `test/README.md` | 0 |
| Q63 | (sub-decisão Q62) | DONE | — | — |
| Q64 | Shared script lib walk-source.ts | DONE | `scripts/lib/walk-source.ts` | 0 |
| Q65 | Shared script lib test-orchestration.sh | DONE | `scripts/lib/test-orchestration.sh` | 0 |
| Q66 | scripts/README.md inventário | PARTIAL | `scripts/README.md` | 7 codemods de `scripts/codemods/` não documentados |
| Q67 | Schemas em .routes.schemas.ts, não inline em .routes.ts | PARTIAL | múltiplos | **10 routes files** ainda têm schemas inline + **7 routes** sem .routes.schemas.ts companion |
| Q68 | (sub-decisão Q67) | DONE | — | — |
| Q69 | (sub-decisão Q66) | DONE | — | — |
| Q70 | test/README.md infraestrutura | DONE | `test/README.md` | 0 |
| Q71 | (sub-decisão Q70) | DONE | — | — |
| Q72 | Reconciliação Q12 vs Q15 (status field para i18n) | DONE | docs/audits/duplication-audit-2026-05-03.md (appendix) | 0 |
| Q73 | Reconciliação Q43 vs Q46 (Prisma vs own enum) | DONE | mesmo | 0 |
| Q74 | UUID v7 strategy: pg_uuidv7 extension | DONE | `prisma/migrations/20260503*` | 0 |
| Q75 | Cache lint warn-then-error transition | DONE | `scripts/lint-cache-direct-delete.ts` (CACHE_LINT_MODE) | 0 (mode=error default, 0 violações) |

### Resumo Q-conformance

- **DONE**: 49 (66%)
- **PARTIAL**: 19 (25%)
- **SKIPPED**: 1 (Q37)
- **DIVERGED**: 0
- **UNKNOWN**: 0

**Top-5 Qs que mais precisam de follow-up:**

1. **Q7** (255 inline `z.string().datetime()`) — mecânico, mas alta volumetria.
2. **Q10** (47 repos com naming antigo) — codemod possível.
3. **Q29** (41 repos sem PrismaLikeClient) — backwards-compatible (PrismaService extends PrismaLikeClient), mas deve ser sweep.
4. **Q37** (0/13 workers usam runWithFailureMode) — port adicionado mas nunca adotado. Maior gap conceitual.
5. **Q67** (17 routes com problemas — inline schemas ou sem companion file).

---

## 1.3 Diff swagger / breaking changes API

### 1.3.1 Paths removidos (BREAKING)

108 endpoints removidos. Categorização:

**Health probes específicos (substituídos por `/health/live` + `/health/ready`):**
- `/api/health/db`, `/api/health/openai`, `/api/health/redis`, `/api/health/smtp`, `/api/health/storage`, `/api/health/translate`

**Legacy `/api/*` (sem `/v1` prefix) — todos movidos para `/api/v1/*`:**
- `/api/accounts`, `/api/accounts/deactivate`
- `/api/auth/2fa/*` (8 endpoints), `/api/auth/login`, `/api/auth/logout`, `/api/auth/refresh`, `/api/auth/reset-password`
- `/api/email-verification/send`, `/api/email-verification/verify`
- `/api/password/change` → **`/api/v1/me/password/change`** (mudou namespace + perdeu auth no swagger — ver P0)
- `/api/gdpr/export` → `/api/v1/me/gdpr-export`
- `/api/chat/*` (12 endpoints)
- `/api/resume-analytics/{resumeId}/*` (10 endpoints)
- `/api/resume-import/*` (8 endpoints)
- `/api/resumes/*` (7 endpoints)
- `/api/search*` (3 endpoints)

**Theme namespace replacement** (`/api/v1/themes/*` → `/api/v1/resume-styles/*`, 9 endpoints).

**Resume parameter rename** (`/api/v1/resumes/{id}/*` → `/api/v1/resumes/{resumeId}/*`).

**ATS endpoints removidos:** `/api/v1/ats/simulate*`, `/api/v1/ats/validate`, `/api/v1/ats/themes/{themeId}/score`.

### 1.3.2 Paths adicionados

132 endpoints novos. Categorização:

- Replacements para os removidos (resume-styles, analytics nested, imports nested).
- New admin endpoints: `/v1/admin/feature-flags*`, `/v1/admin/fit-questions*`, `/v1/admin/access-modifiers*`.
- New auth: `/v1/auth/email-verification/resend-status`, `/v1/auth/oauth/providers`.
- New features: `/v1/career-graph/view`, `/v1/match`, `/v1/recruiting/*`, `/v1/feature-flags/*`, `/v1/fit-profile/*`, `/v1/i18n/dictionary/*`.
- Health: `/api/health/live`, `/api/health/ready`.

### 1.3.3 Schemas breaking

#### P0 — Login response perdeu accessToken/refreshToken/expiresIn

**Antes** (`client-swagger.json` em `838b1657`):
```json
{
  "accessToken": "string",
  "refreshToken": "string",
  "expiresIn": "number",
  "userId": "string",
  "twoFactorRequired": "boolean?"
}
```

**Depois** (HEAD):
```json
{
  "anyOf": [
    { "userId": "string", "twoFactorRequired": true },
    { "userId": "string" }
  ]
}
```

**Investigação manual necessária:** verificar se o handler real retorna tokens (e o swagger gerador é que está errado) OU se de fato o login agora só retorna userId (e tokens vão para cookie httpOnly). Se for esse o caso, frontend tem que ler o cookie ou abandonar `Authorization: Bearer` e usar cookies. **Bloqueador de merge até resolver.**

#### P0 — POST /v1/me/password/change sem auth

**Antes:** `auth: bearer` (required).
**Depois:** `auth: null` (público).

Ou (a) o swagger generator perdeu o requisito de auth (bug do generator), ou (b) o refactor de routes de fato perdeu o `auth: { kind: 'jwt' }` no descriptor. **Auditar `password-management.routes.ts:84` antes de mergear.**

#### P0 — components.schemas vazio

**Antes:** 271 schemas em `components.schemas`.
**Depois:** 0 schemas. Tudo inline.

Impact: clients usando codegen do swagger vão gerar tipos inflados, perdem `$ref` reuse. Pode ser intencional (Elysia gera inline) mas degrada DX. **P1.**

#### P2 — POST endpoints retornando 200 em vez de 201

Q17 declara default 201 para POST. Swagger mostra TODOS os POSTs com 200. Pode ser bug do generator OU os route descriptors declaram `statusCode: 200` explicitamente. Verificação manual (16 POSTs que de fato declaram 200, em sua maioria legítimos: login/logout/upload/etc).

### 1.3.4 Pagination shape change

Routes que migraram para `Q1` envelope passaram de `{ data, meta }` para `{ items, total, page, limit, totalPages, hasNext, hasPrev }`. Frontend precisa adaptar. Lista parcial: `/v1/jobs`, `/v1/social/*`, `/v1/feed/timeline`. **Confirmar com frontend antes do merge.**

---

## 1.4 Diff schema Prisma — migrations e modelos

### 1.4.1 Migrations introduzidas pelo PR

| Migration | Risco | Resumo |
|---|---|---|
| `20260421165430_add_refresh_token_auth_method` | P3 | ADD COLUMN `authMethod TEXT` em RefreshToken (nullable) |
| `20260423012030_feature_flags` | P2 | CREATE TABLE FeatureFlag + FeatureFlagDependency, ADD enum value FEATURE_FLAG_TOGGLED |
| `20260423221242_scoring_refactor` | **P1** | 303 linhas — drops ResumeTheme + scoring columns, creates ResumeStyle, FitProfile, FitQuestion, FitAnswer, embeddingVector vector(1536). Cria trigger monotônico em styleScore. **Não-reversível** (data copy). |
| `20260424000228_scoring_notification_types` | P3 | ADD 5 NotificationType enum values |
| `20260425001500_drop_resume_email_contact` | **P1** | DROP COLUMN Resume.emailContact (sem backfill — assume User.email povoado) |
| `20260430040810_authz_refactor` | **P0** | DROP TABLE Group/UserGroup/GroupPermission/GroupRole/UserPermission. CREATE TABLE AccessModifier. **Sem export ou backfill — perde memberships/permissions.** Locks agressivos em prod. |
| `20260430102612_drop_has_completed_onboarding` | **P1** | DROP COLUMN User.hasCompletedOnboarding (substituído por onboardingCompletedAt — código deve estar pre-updated) |
| `20260503120000_install_pg_uuidv7` | **P0** | CREATE EXTENSION pg_uuidv7. **Requer super-user + Postgres allowlist.** Sanity check incluído. |
| `20260503130000_swap_id_defaults_to_uuidv7` | P2 | ALTER 51 tabelas DEFAULT id → uuidv7()::text. **Zero-downtime** (metadata only). Linhas existentes mantêm v4. |

### 1.4.2 Schemas modificados (resumo)

**Adicionados:**
- `AccessModifier` (substitui Group hierarchy)
- `ResumeStyle` (renomeado de ResumeTheme, dados preservados via INSERT INTO)
- `ResumeQualityScoreHistory`, `UserFitProfile`, `JobFitProfile`, `FitQuestion`, `FitQuestionSet`, `FitAnswer`, `FitRemapHistory`
- `FeatureFlag`, `FeatureFlagDependency`

**Removidos:**
- `Group`, `GroupRole`, `GroupPermission`, `UserGroup`, `UserPermission`
- `ResumeTheme`

**Campos adicionados:**
- `RefreshToken.authMethod` (nullable)
- `Job.culturalProfileCaptured`, `embeddingVector`, `enrichedAt`, `enrichedBy`, `requirementsEnrichedByAi`, `requirementsStructured`
- `User.onboardingCompletedAt`
- `Post.deletedAt`, `Message.deletedAt` (soft-delete markers)
- `AccessModifier.*` (12 fields)

**Campos removidos:**
- `Resume.template` (era ResumeTemplate enum), `Resume.emailContact`
- `User.hasCompletedOnboarding`
- `resume_analytics.atsScore/completenessScore/improvementSuggestions/industryRank/keywordScore/missingKeywords/topKeywords/totalInIndustry`

### 1.4.3 UUID v7 phase 2 — segurança verificada

- 51 tabelas afetadas
- Todas as colunas `id` permanecem `String`/TEXT (sem conversão de tipo).
- FK references não quebram (todos os targets também são TEXT).
- Sanity check incluído na migration (verifica `pg_uuidv7` carregado via DO block).
- Old rows mantêm v4 (cuid()-style); new rows pegam v7.
- **Verificação operacional:** confirmar que `pg_uuidv7` está na allowlist do Postgres em produção (RDS, Heroku, etc).

### 1.4.4 Soft-delete coverage

- `SOFT_DELETE_MODELS = ['Message', 'Post', 'PostComment']` em `bounded-contexts/platform/prisma/soft-delete.ts`.
- `Message`: tem `isDeleted` + `deletedAt` ✓
- `Post`: tem `isDeleted` + `deletedAt` ✓
- `PostComment`: tem `isDeleted` mas **falta `deletedAt`** (P2 inconsistência).
- Nenhum outro model tem `isDeleted` — não há leak (✓).

---

## 1.5 Pacote `packages/i18n`

### 1.5.1 Surface API

Exports adicionados pelo PR:
- `SuccessMessageCode`, `SuccessMessageDictionary`, `SuccessMessageTemplate`, `renderSuccessMessage`, `SUCCESS_MESSAGE_DICTIONARY` (Q8).
- `INTERNAL_TRANSLATION_MISSING`, `INTERNAL_UNSUPPORTED_LOCALE`, `INTERNAL_MALFORMED_TRANSLATION` (Q16).

### 1.5.2 Diff de dictionaries

**Codes adicionados** (último commit `5703d8d7`, i18n parity sweep):
- `CANDIDATE_DIRECTORY_UNAVAILABLE`, `CANDIDATE_POOL_EMPTY`
- `CAREER_COHORT_EMPTY`, `CAREER_GRAPH_INVALID_MAX_BUCKETS`, `CAREER_GRAPH_REPOSITORY_UNAVAILABLE`, `CAREER_GRAPH_STACK_REQUIRED`
- `MATCH_CANDIDATES_INVALID_LIMIT`, `MATCH_CANDIDATES_NO_CRITERIA`

**Codes órfãos removidos (10):**
- `ALREADY_ENDORSED`, `CANNOT_ENDORSE_SELF`, `CANNOT_RECOMMEND_SELF`, `COLLABORATOR_SELF_INVITE`, `COMMENT_NOT_OWNED`, `ONBOARDING_NOT_COMPLETED`, `ONBOARDING_STEP_OUT_OF_ORDER`, `RECOMMENDATION_ALREADY_WRITTEN`, `REPORT_ALREADY_SUBMITTED`, `REPORT_NOT_REVIEWABLE`

Architecture parity tests passam (69 pass / 1 skip / 0 fail).

---

## 1.6 Diff de dependências (`bun.lock`)

Não houve mudanças significativas em deps — refactor é majoritariamente in-place. Pacotes notáveis ainda em uso (e potencial CVE attention):

- `puppeteer`/`puppeteer-extra` — para PDF rendering. Histórico de CVEs em `chromium`. **Recomendação:** rodar `bun pm audit` periodicamente.
- `openai` — API key handling. Verificar não vaza em logs.
- `pdf-parse` — usado em resume import. Capped em 5MB no código (✓).
- `mammoth`, `docx`, `jszip` — file parsing. Sem CVEs conhecidos atualmente.
- `nodemailer` — verificar se fica em bundle de prod (deveria, é runtime).

`@aws-sdk/*` (S3 client + presigner) — versão usa cast `as any` em `s3-upload.service.ts:280` para resolver divergência de types `@smithy/types` entre sub-deps. Funciona, mas frágil em upgrades.

---

## 1.7 Endpoints novos do homolog: portados? perdidos?

PR #213 (backend audit hardening — 30 fixes), PR #211 (release pipeline hardening), PR #224 (sharded e2e runner) foram mergeados em `homolog` enquanto `refactor/backend` rodava. O merge bucket strategy (commit `e1fa9073`) usou `git merge -X ours` (refactor wins) com overrides per-bucket.

**Bucket 9 (test/) — `take homolog`** garantiu que sharded runner foi preservado.

**Riscos residuais:**
- Buckets 4-8 (routes, use-cases, adapters, repositories, schemas) usaram refactor's version. Adições do homolog NESSES arquivos podem ter sido descartadas silenciosamente.
- A análise do diff por arquivo seria ideal mas é tooling-heavy (3-way diff por arquivo). Recomendação: rodar uma comparação focada em arquivos onde o homolog teve commits (PR #213 fixes).

PRs específicas a verificar:
- `4a91012d fix(backend): backend audit hardening — 30 fixes` — confirmar que os 30 fixes de hardening não foram acidentalmente revertidos.
- `13870b64 feat(integration): port e2e's sharded runner` — sharded runner está vivo (verificado na seção test).

---

# Parte 2 — Achados ordenados por severidade

## P0 — Bloqueadores / risco crítico em prod

### P0-001 [PRE] Security: JWT_SECRET com default inseguro

**Arquivo:** `src/infrastructure/elysia-adapter/elysia-bootstrap.ts:146`
```ts
secret: config.getOrDefault<string>('JWT_SECRET', 'dev-secret-change-me')
```
Se `JWT_SECRET` não estiver definido em prod, o JWT é assinado com `'dev-secret-change-me'` — qualquer atacante com conhecimento desse default forja tokens válidos.
**Fix:** Remover o default; lançar erro no boot se ausente. Alternativa: `config.getOrDefault('JWT_SECRET', null) ?? throwBootError(...)`.

### P0-002 [PRE] Security: WebSocket sem origin validation (CSRF surface)

**Arquivo:** `src/infrastructure/elysia-adapter/elysia-websocket.adapter.ts:131-145`
WS upgrade não valida header `Origin`. Página em `attacker.com` pode abrir `wss://app.example.com/ws/chat` — browser envia cookie httpOnly automaticamente, server autentica.
**Fix:** Em `beforeHandle`, comparar `ctx.request.headers.get('origin')` com whitelist. Reject 403 se inválido.

### P0-003 [PR] Security: POST /v1/me/password/change perdeu auth no swagger

**Arquivo:** `src/bounded-contexts/identity/password-management/password-management.routes.ts` (route descriptor) + `client-swagger.json`
Antes do refactor: `auth: bearer required`. Depois: `auth: null`. Pode ser bug do generator OU o descriptor de fato esqueceu `auth: { kind: 'jwt' }`. **Bloqueia merge até confirmar via teste manual em staging.**

### P0-004 [PRE] Authz: Endpoints de export permitem download de qualquer resume

**Arquivos:**
- `src/bounded-contexts/export/export.routes.ts:117-137` (`GET /v1/export/user/:userId/resume/pdf`)
- `src/bounded-contexts/export/export.routes.ts:172-201` (`GET /v1/export/:resumeId/json`, `/v1/export/:resumeId/latex`)

Routes têm `permission: Permission.RESUME_EXPORT` mas não checam ownership. `ResumeDataRepository.findForJsonExport(resumeId)` faz `findUnique({ where: { id: resumeId } })` — sem filtro de userId.
**Cenário de ataque:** User A autenticado conhece resumeId de User B → `GET /v1/export/<user-b-resume-id>/json` → backend gera o resume, presigned URL, download.
**Fix:** Adicionar guard de ownership ou repo método `findForJsonExportByUser(resumeId, userId)`.

### P0-005 [PRE] Authz: DELETE /v1/upload/file/:key sem ownership check (BUG-027 conhecido)

**Arquivo:** `src/bounded-contexts/integration/upload/application/use-cases/delete-upload/delete-upload.use-case.ts:7`
Use case literalmente comenta: "NOTE: there is NO ownership check here. The legacy security spec documents this as a known gap (BUG-027)".
**Fix:** Validar que `key.split('/')[1] === userId` (S3 keys encodam userId no path).

### P0-006 [PR] Contract: POST /v1/auth/login swagger perdeu accessToken/refreshToken/expiresIn

**Arquivo:** `client-swagger.json` (path `/api/v1/auth/login`, response 200/201)
Anteriormente retornava `{ accessToken, refreshToken, expiresIn, userId, twoFactorRequired? }`. Agora swagger mostra `anyOf: [{userId, twoFactorRequired:true}, {userId}]` — sem tokens.
**Investigação:** Verificar se o handler real ainda retorna tokens (e o swagger generator é que está bugado), OU se o login mudou para set-cookie + retorno só do userId.
**Bloqueia merge** até resolver com o frontend team.

### P0-007 [PRE] PII: Email logged em plain text em UserMutationRepository

**Arquivo:** `src/bounded-contexts/identity/users/infrastructure/adapters/persistence/user-mutation.repository.ts:29`
```ts
this.logger.log(`Creating user account: ${userData.email}`, CTX);
```
Em prod, vira campo indexável nos logs JSON — viola GDPR/LGPD.
**Fix:** Trocar `${userData.email}` por `${userId}`, ou redigir.

### P0-008 [PRE] PII: Email recipient logged em EmailSenderService

**Arquivo:** `src/bounded-contexts/platform/common/email/services/email-sender.service.ts:62, 77`
Logs `to: options.to` em modo info/warn.
**Fix:** Redigir email (`to: redact(options.to)`) ou remover do log.

### P0-009 [PR] Migration risk: authz_refactor dropa Group sem export

**Arquivo:** `prisma/migrations/20260430040810_authz_refactor/migration.sql`
Drops `Group`, `UserGroup`, `GroupPermission`, `GroupRole`, `UserPermission`. Membros e permissions herdadas via grupo são **perdidas sem audit trail**. Aplicação deve ter sido pre-updated para usar `roles[]` (default empty) e `AccessModifier`.
**Verificação pré-deploy:** dump das tabelas em staging ANTES de aplicar a migration. Confirmar que nenhum user em prod depende de membership de grupo.

### P0-010 [PRE] Workers: TimeCapsule e AntiGhosting enviam emails duplicados em multi-instance

**Arquivos:**
- `src/bounded-contexts/resumes/time-capsule/time-capsule.worker.ts` (cron `30 8 * * *`)
- `src/bounded-contexts/jobs/infrastructure/workers/anti-ghosting.worker.ts` (cron `0 9 * * *`)

Ambos rodam em cada pod ao mesmo tempo. Sem `DistributedLockPort.withLock()`. **2+ pods = emails duplicados** para users.
**Fix:** Wrap em `lock.withLock(\`<worker-name>\`, { ttlMs: 300000 }, () => ...)`.

### P0-011 [PR] Migration risk: pg_uuidv7 requer super-user + allowlist

**Arquivo:** `prisma/migrations/20260503120000_install_pg_uuidv7/migration.sql`
`CREATE EXTENSION pg_uuidv7` falha em managed Postgres (RDS, Heroku, GCP CloudSQL) se a extensão não estiver na allowlist (`shared_preload_libraries` ou similar).
**Verificação pré-deploy:** confirmar que extensão está disponível na infra. Sanity check no SQL faz fail-fast (✓).

### P0-012 [PR] Migration risk: drop_resume_email_contact assume User.email povoado

**Arquivo:** `prisma/migrations/20260425001500_drop_resume_email_contact/migration.sql`
`ALTER TABLE Resume DROP COLUMN emailContact` (com `IF EXISTS` guard). Não há backfill — assume que o app já lê de `User.email`. Se algum código residual ler `resume.emailContact`, runtime crash.
**Verificação:** grep `emailContact` no src/ e confirmar 0 uses antes do deploy.

### P0-013 [PRE] SSRF em fetch-link-preview.adapter.ts

**Arquivo:** `src/bounded-contexts/feed/infrastructure/adapters/external-services/fetch-link-preview.adapter.ts:26`
URL submetida pelo user em post creation é fetched server-side sem validação. Atacante pode scan internal services (127.0.0.1, 10.0.0.0/8, link-local 169.254.169.254 do AWS metadata, etc), ou usar protocolos `file://`, `data://`.
**Fix:** Validar URL antes do fetch — bloquear private IPs, exigir https/http only.

### P0-014 [PRE] SSRF em webhook delivery

**Arquivo:** `src/bounded-contexts/platform/webhooks/infrastructure/adapters/external-services/http-webhook-delivery.adapter.ts:59`
URL registrada pelo user no webhook config é POST'd sem allowlist. Mesmo cenário do P0-013.
**Fix:** Validar URL no momento de registro (não só no delivery) e na hora do delivery (defense in depth).

### P0-015 [PRE] S3 buckets públicos por default

**Arquivo:** `src/bounded-contexts/platform/common/services/s3-upload.service.ts:117`
Profile images, company logos, post images são uploaded com `ACL: 'public-read'`. Quem souber/adivinhar a URL acessa sem auth. Aceitável SE intencional (perfis públicos), mas:
- Não está documentado em CLAUDE.md.
- Profile photo de user pode incluir face (privacy regulation).
**Fix:** Documentar política de ACL em CLAUDE.md OU mover para private + proxy via app.

### P0-016 [PRE] Test coverage zero em 9 BCs com 31+ use cases

**BCs afetadas:**
- `feed` (21 UCs, 0 specs)
- `jobs` (23 UCs, 0 specs)
- `fit-profile` (12 UCs)
- `notifications` (13 UCs, 1 service)
- `resume-styles` (7 UCs)
- `success-stories` (4 UCs)
- `career-graph` (1 UC)
- `recruiting` (1 UC)
- `resume-quality` (3 UCs)
- `automation` (4 UCs, 1 service)

Risco: bugs nesses fluxos não são detectados antes de prod. Pode ter shipping de regressões silenciosas.
**Fix:** Adicionar baseline integration specs para cada UC crítico. Estimativa: 30-50 specs novos.

### P0-017 [PRE] JobUpdatedEvent publicado mas sem handler — match cache stale

**Arquivos:**
- Publisher: `src/bounded-contexts/jobs/application/use-cases/update-job/update-job.use-case.ts:35`
- Expected handler: deveria existir em `src/bounded-contexts/job-match/infrastructure/handlers/`

Quando admin/recruiter edita um job (requirements, keywords, etc), o `MatchCachePort` para aquele job não invalida → users continuam vendo match scores stale por até 24h (TTL).
**Fix:** Criar `JobMatchRecomputeOnJobUpdatedHandler` que enqueue `{ kind: 'invalidate-job', jobId }` na queue de match recompute.

---

## P1 — Corrigir esta semana

### Tests (Wave 4)

- **P1-018** [PRE] 13 instâncias de `setTimeout(r => r, 500ms+)` em testes — flake risk em CI lento. Replace por polling.
- **P1-019** [PRE] 166 instâncias de `if (setupFailed) return` em integration tests — silently passing. Throw em `beforeAll` em vez de return.
- **P1-020** [PRE] Email generation `${Date.now()}-${Math.random()}` em `authentication.spec.ts:50,60,70` — collision possible em parallel. Use UUID.

### HTTP layer (Wave 4)

- **P1-021** [PR] DELETE routes inconsistentes — `/v1/auth/sessions/:id` e `/v1/upload/file/:key` retornam 200 com body em vez de 204 No Content. Standardize.
- **P1-022** [PRE] Internal exceptions extending raw `Error` (ThemeCompilationError, FitAnswerCountMismatchError, FlagGraphCycleError) — quando wrapped em DomainException, perde `cause`. Adicionar suporte a `{ cause }` em DomainException.

### Observability (Wave 4)

- **P1-023** [PRE] 5 métricas Prometheus definidas mas NUNCA incrementadas:
  - `resume_created_total` (resumeCreated)
  - `user_signup_total` (userSignup)
  - `api_latency_seconds` (apiLatency — middleware HTTP nunca observa)
  - `active_users_total` (activeUsers — login/logout não instrumenta)
  - `pending_exports_total` (pendingExports)
  Files: `src/bounded-contexts/platform/metrics/internal/metrics-registry.factory.ts:26-88`. **Fix:** Wire handlers ou remover.

### Performance (Wave 3)

- **P1-024** [PRE] Missing index `@@index([userId, createdAt])` em `PostLike` (full table scan em listReactionsByUser).
- **P1-025** [PRE] Missing index `@@index([userId, createdAt])` em `PostBookmark`.
- **P1-026** [PRE] Cache stampede em `GetShareBySlugUseCase` (`src/bounded-contexts/presentation/public-resumes/application/use-cases/get-share-by-slug.use-case.ts:28-57`) — 10 requests concorrentes para mesmo slug = 10 recomputes. Use `cache.getOrSet()` ou `withLock`.
- **P1-027** [PRE] Cache stampede em `ComputeMatchUseCase` (`src/bounded-contexts/job-match/application/use-cases/compute-match.use-case.ts:74-112`) — cada miss dispara 4 AI calls. Concorrentes = 4×N AI calls. Custo $$$.
- **P1-028** [PRE] Feed timeline (`ListFeedTimelineUseCase`) sem cache. Hot path no app load. Cache de 10-30s reduziria DB load significativamente.

### Security (Wave 3)

- **P1-029** [PRE] CORS `origin: true` em dev quando `CORS_ORIGIN` env var vazia (`src/infrastructure/elysia-adapter/elysia-bootstrap.ts:705`) — aceita qualquer origin em dev.
- **P1-030** [PRE] `/v1/auth/forgot-password` sem rate limit — brute-force / email enumeration / spam.
- **P1-031** [PRE] Direct `process.env.X` em código fora de ConfigPort — 14 callsites (BCRYPT_COST, REDIS_HOST, NODE_ENV, LOG_LEVEL, etc). Bypass de validação centralizada.
- **P1-032** [PRE] APP_URL fallback para `http://localhost:3000` em emails de password reset (`src/bounded-contexts/identity/password-management/infrastructure/adapters/external-services/email-password-reset.sender.ts:22`) — em prod sem APP_URL definida, links de reset são quebrados.

### Workers (Wave 7)

- **P1-033** [PRE] Q37 — 0/13 workers usam `runWithFailureMode`. Failure mode implícito em try/catch. Wire o helper.
- **P1-034** [PRE] BullMQ retry config inconsistente — só platform queues têm `attempts: 3 + exponential backoff`. Outras 10+ queues falham na 1ª tentativa (`attempts: 0` default).

### Events (Wave 7)

- **P1-035** [PRE] 14 dead `DomainEvent` classes — publicados mas sem handler:
  - `ConnectionRequestedEvent`, `ExportCompletedEvent`, `ExportFailedEvent`, `ExportRequestedEvent`, `LoginFailedEvent`, `SessionCreatedEvent`, `SessionTerminatedEvent`, `ShareDownloadedEvent`, `TokenRefreshedEvent`, `UserFollowedEvent`, `UserLoggedInEvent`, `UserLoggedOutEvent`, `VersionCreatedEvent`, `VersionRestoredEvent`.
  Decisão necessária: adicionar handler ou remover.
- **P1-036** [PRE] 15 handlers sem try/catch — silently lost errors. Lista parcial: `ResumePublishedActivityHandler`, `CleanupSocialOnUserDeleteHandler`, `MutualFollowOnConnectionAcceptedHandler`, `InvalidateCacheOnResumeUpdate`, `InvalidateCacheOnResumeDelete`, `CleanupResumesOnUserDeleteHandler`, `ShareEventHandler.handleShareViewed`, etc.
- **P1-037** [PRE] `UserDeletedEvent` ordering: 2 handlers (cleanup-social + cleanup-resumes) podem rodar concorrentes; se houver FK constraint, deletion falha. Document or refactor.

### WebSocket (Wave 7)

- **P1-038** [PRE] 4 handlers de chat sem `validateWsMessage` (Q39 partial): `typing:start`, `typing:stop`, `message:read`, `conversation:join`, `conversation:leave`. Payloads `unknown` acessados sem narrow.
- **P1-039** [PRE] WS sem rate limit per-connection (`src/infrastructure/elysia-adapter/elysia-websocket.adapter.ts:164`). 1 client → 100+ msg/s sem limite.
- **P1-040** [PRE] WS sem max message size — atacante envia 10MB JSON, blocking loop em parse.
- **P1-041** [PRE] WS broadcast O(N) síncrono — sala com 500 users = 500 sends sequenciais.
- **P1-042** [PRE] WS user status broadcast sem debounce — disconnect/reconnect rápido = spam para 100+ rooms.
- **P1-043** [PRE] Conversation handlers sem isParticipant check: `typing:stop` (chat.handler.ts:184), `conversation:leave` (chat.handler.ts:231).
- **P1-044** [PRE] CSRF on WS via httpOnly cookie session — sem origin validation, browser auto-envia cookie cross-site.

### i18n (Wave 8)

- **P1-045** [PR] 16 routes retornam hardcoded EN strings em vez de SuccessMessageEnvelope:
  - `users.routes.ts:165,228,354,375,394,416,437` (7 mensagens)
  - `email-verification.routes.ts:37,55` (2)
  - `account-lifecycle.routes.ts:101,126` (2)
  - `password-management.routes.ts:40,63,84` (3)
  - `resume-analytics.routes.ts:66`
  - `resumes/core/resumes.routes.ts:324`
  Users em pt-BR veem inglês.

### Architecture (Wave 6)

- **P1-046** [PRE] `automation/run-rage-apply.use-case.ts:11` importa direto de `@/bounded-contexts/resumes/resume-versions/application/services/resume-tailor.service` — viola BC boundary. Criar port + injetar via composition.
- **P1-047** [PRE] 11 dependency cycles entre platform e várias BCs (export, social, presentation, job-match, resume-quality). Platform é hub mas precisa quebrar dependência inversa via ports.
- **P1-048** [PRE] 405 cross-BC imports violam boundaries (211 são imports de PrismaService — abstrair via port).

### TS strictness (Wave 6)

- **P1-049** [PRE] `tsconfig.json` falta 4 strict flags críticas:
  - `noUncheckedIndexedAccess: true` — exporia 211+ non-null assertions
  - `exactOptionalPropertyTypes: true`
  - `noImplicitOverride: true`
  - `noPropertyAccessFromIndexSignature: true`
  Impacto: catches reais bugs hoje invisíveis.
- **P1-050** [PRE] `noFallthroughCasesInSwitch: false` explicitamente — switch fall-through bugs passam silently.
- **P1-051** [PRE] `error.mapper.ts:39` usa `(exception as unknown as Record<string, unknown>)[key]` — fragile. Usar property allowlist.
- **P1-052** [PRE] 40+ `ctx.user!` em routes — assume auth middleware sem provar no tipo. Introduzir `AuthenticatedContext<T>`.

### Repos (Wave 10)

- **P1-053** [PRE] Q10 — 14 repos têm `findAll*` (deveriam ser `list*`). Lista em §3.3.3.
- **P1-054** [PRE] `conversation.repository.ts:15-44` — race condition `findOrCreate` sem TX → duplicate conversation. Use `upsert` com unique key.

### Use cases (Wave 10)

- **P1-055** [PRE] Missing TX em `create-account.use-case.ts:64-84` — User + 2 consents + tokens sem `runInTransaction`. Risco LGPD se 2º consent falhar.
- **P1-056** [PRE] Missing TX em `send-message-to-conversation.use-case.ts:48-60` — message + updateLastMessage sem TX. UI mostra last message stale.
- **P1-057** [PRE] Missing TX em `restart-onboarding.use-case.ts:54-89` — delete progress + upsert progress + update user, sem TX.
- **P1-058** [PRE] 87/375 use cases (~23%) sem spec test.

### Storage (Wave 5)

- **P1-059** [PRE] Post image upload (`upload-post-image.use-case.ts`) sem magic bytes validation — só MIME header. Atacante pode enviar executable com fake MIME.
- **P1-060** [PRE] Sem cleanup worker para orphan blobs no S3 — failed DB writes deixam files perdidos. Acumula custo storage.

### Cache (Wave 5)

- **P1-061** [PRE] `JobUpdatedEvent` não invalida match cache (duplicado P0-017 — listado também em P1 para visibilidade no perf bucket).

### Routes / API contract

- **P1-062** [PR] Swagger `components.schemas` vazio — clients usando codegen perdem `$ref` reuse, geram tipos inflados.

### Cron sem distributed lock (extra dos P0)

- **P1-063** [PRE] `SkillDecayWorker` sem lock — wasteful em multi-instance (full table scan x N pods) mas safe (idempotent).
- **P1-064** [PRE] `ViewsProjectionWorker` sem lock — concurrent upserts safe mas wasteful.

### Q-conformance partial (extra)

- **P1-065** [PRE] Q22 violation: `translation/application/compositions/translation.composition.ts:20-23` usa inline `console.log/warn/error` em vez de LoggerPort.
- **P1-066** [PRE] Q28 violation: 22 chamadas `$transaction` direto fora de `runInTransaction`.

### Schema Prisma

- **P1-067** [PRE] PostComment falta `deletedAt` field (tem `isDeleted` mas não data) — quebra audit trail.
- **P1-068** [PRE] `Resume.style.styleScore` tem trigger BD-level monotônico — qualquer código tentando baixar score raise exception. Não documentado em CLAUDE.md, surpreende reviewers.

### Scripts

- **P1-069** [PRE] `scripts/mec-download.ts:130-131` deleta CSV antes de baixar nova — se download falha, perde original. Atomic via temp file.
- **P1-070** [PRE] `scripts/seed-i18n-catalogs.ts:137` overwrites dictionary sem backup nem `--dry-run`.
- **P1-071** [PRE] `scripts/i18n-audit.ts` sem try-catch wrapping main() — silent failure se walk/read falha.

### Auth / 2FA

- **P1-072** [PRE] `bcrypt-hash.adapter.ts:9` para 2FA backup codes usa cost 10 (vs 12 para passwords) — inconsistente.
- **P1-073** [PRE] Login response missing tokens (duplicado P0-006).

### Outros

- **P1-074** [PRE] Translator de realtime sem locale context — `TranslateAndPublishUseCase` não diferencia en/pt-BR.
- **P1-075** [PRE] Translator errors swallow domain events — handler buggy = events perdidos sem DLQ.
- **P1-076** [PRE] Workers sem timeout em external calls (LLM, SMTP) — hang indefinido bloqueia worker thread.
- **P1-077** [PRE] Workers não wired em `OnShutdownPort` (Q36 partial) — partial writes em SIGTERM.
- **P1-078** [PR] Feature-flag guard declarado em `export.routes.ts:70` mas guard class não existe em src — runtime error quando guard middleware roda.

---

## P2 — Sprint atual

(Lista resumida — detalhes nos waves correspondentes em Parte 3.)

### Q-conformance partial (Wave 2)
- **P2-079** Q5 — ~20 callsites inline `z.object({ id: z.string() })` em vez de `IdParamSchema`.
- **P2-080** Q7 — 255 inline `z.string().datetime()` em vez de `IsoDateTimeSchema`.
- **P2-081** Q10 — 47 repos com naming antigo (codemod possível).
- **P2-082** Q27 — `platform/i18n/domain/translation.port.ts:15` ordem inconsistente `['pt-BR', 'en']`.
- **P2-083** Q29 — 41 repos sem PrismaLikeClient.
- **P2-084** Q31 — 6 const TTL locais migráveis para CACHE_PRESETS.
- **P2-085** Q57 — ~25 testes ainda usam `create*` / `fixture*` em vez de `build*`/`freshInDb*`.
- **P2-086** Q67 — 10 routes com schemas inline + 7 routes sem .routes.schemas.ts companion.

### Naming (Wave 6)
- **P2-087** 17 adapters com class name fora do padrão (OpenAI vs Openai, BullMQ vs Bullmq, OAuth vs Oauth, etc).
- **P2-088** 2 arquivos `.adapter.ts` sem class export (deveriam ser `.util.ts`): `section-projection.adapter.ts`, `cookie-bridge.adapter.ts`.

### TS strictness (Wave 6)
- **P2-089** 218 ocorrências `as unknown as X` (double cast) — many em test fixtures (OK), alguns em prod code (smell).
- **P2-090** Optional vs nullable inconsistente em DTOs (User.preferences, Resume.content, Resume sections.meta).

### Architecture (Wave 3)
- **P2-091** 5 routes files > 450 linhas: jobs.routes.ts (573), onboarding.routes.ts (528), admin-catalog.routes.ts (478), feed.routes.ts (473), resumes/core/resumes.routes.ts (457). Split sugerido.

### Cache (Wave 5)
- **P2-092** Cache warming TTLs muito curtos (2m profile, 5m public resume) — warming inefetivo.
- **P2-093** Auth `DEFAULT_CACHE_TTL_SECONDS = 60` duplicado em 2 lugares; deveria usar `CACHE_PRESETS.EPHEMERAL`.

### Mappers (Wave 5)
- **P2-094** `toConsentHistoryResponse` (sem suffix Dto), `toPinnedRepos` (genérico) — Q9 partial.
- **P2-095** Type casts unsafe: `to-response-dto.ts:18-21` (4 casts as Record<string, unknown>), `resume-style.presenter.ts:30` (double cast `as unknown as Record<string, number>`).
- **P2-096** Mapper colocation: `resumes/section-types/application/to-response-dto.ts` deveria estar em `infrastructure/presenters/`.

### Storage (Wave 5)
- **P2-097** Sem export metadata table no DB para presigned exports — orphan files acumulam.

### Workers / events (Wave 7)
- **P2-098** Event versioning ausente — `DomainEvent` base class sem `schemaVersion` field. Future migration risk.
- **P2-099** EventBus in-process only — multi-instance perde eventos entre pods. Documentar deployment constraint.
- **P2-100** WS reconnect sem missed message recovery — clientes precisam fazer poll manual via HTTP.
- **P2-101** WS duplicate room subscriptions on rapid reconnect.
- **P2-102** Per-user WS connection cap missing.

### Domain (Wave 8)
- **P2-103** TranslationCoreService faz `fetch()` direto — domain service não deveria. Extrair `TranslationApiPort`.
- **P2-104** `resumes/variants/application/ports/variant-repository.port.ts` em wrong layer — deveria ser `domain/ports/`.
- **P2-105** 5 pares de events duplicados byte-by-byte (analytics, collaboration, presentation duplicates entre `domain/events` e `*/shared-kernel/domain/events`).
- **P2-106** Primitive obsession: Hashtag, JobTitle, Slug, Locale, Currency, EnglishLevel, RemotePolicy faltam value objects.

### Flags (Wave 8)
- **P2-107** FeatureFlag schema sem `owner` e `sunsetDate` fields. Adicionar.
- **P2-108** Hardcoded literal `'resumes.export.pdf'` em export.routes.ts:70 — extrair para const.
- **P2-109** Sem suporte a per-percentage rollout (A/B testing).

### Cron / dist lock
- **P2-110** Outros 4 crons (SkillDecay, TimeCapsule, ViewsProjection, AntiGhosting) sem lock — wasteful em multi-instance, mas mostly safe via DB constraints.

### i18n usage (Wave 8)
- **P2-111** Fallback section labels hardcoded EN em `locale-resolver.util.ts:110-112`, duplicados em 2 outros arquivos.
- **P2-112** Date formatting hardcoded `pt-BR` em password-changed.template.ts:9 e hardcoded `en-US` em export-latex.use-case.ts:262 e generic-docx-section.builder.ts:133.
- **P2-113** Plural rules ad-hoc em 2 arquivos — works for en+pt-BR mas não escalável.

### Repos (Wave 10)
- **P2-114** ~20 queries Prisma sem `select:`/`include:` — fetch de colunas desnecessárias.
- **P2-115** `RESUME_RELATIONS_INCLUDE` sem `take:` em sections + items — pode fetch 1000+ items.
- **P2-116** 41 repos com constructor `PrismaService` em vez de `PrismaLikeClient` (Q29 partial).
- **P2-117** 54/95 repos sem spec test.

### Use cases (Wave 10)
- **P2-118** LaTeX export use case (266 lines) — refatorar template strategy.
- **P2-119** UCs duplicados parciais: 2x `reset-password.use-case.ts` (password-management vs users), 7 `create-*-language.use-case.ts` repetitive.
- **P2-120** 12 UCs com authZ inline (ownership checks) — design choice ok mas inventory.

### Config (Wave 10)
- **P2-121** Email URLs fallback localhost em prod risk — fail-fast se FRONTEND_URL ausente em prod.
- **P2-122** SKIP_EMAIL_VERIFICATION lido direto via process.env.
- **P2-123** Tests lendo process.env diretamente (admin email/password) — usar TestApp fixture.

### Scripts (Wave 9)
- **P2-124** `scripts/codemods/` (7 files) não documentados em README.
- **P2-125** `check-file-size.ts snapshot` mode silently passes — adicionar confirmação.

### Repo config (Wave 9)
- **P2-126** Biome `noUnusedVariables: warn` em test files (deveria ser error).
- **P2-127** Biome `noRestrictedImports: off` em handler files — permite NestJS imports. Documentar.
- **P2-128** Env vars `SENDGRID_API_KEY`, `INTERNAL_API_TOKEN` não estão em `.env.example`.
- **P2-129** Atestation witness key format unclear no `.env.example` (line 129 mostra `\n` literal).
- **P2-130** package.json scripts: `test:debug` duplicate de `test:unit`, `db:*` e `prisma:*` overlap.

### CI workflows (Wave 9)
- **P2-131** `ci.build.yml` com `push: false` — só builds, não pusha. Confirmar intent.
- **P2-132** Workflow integration timeout: 15min, e2e: 20min — appropriate mas monitor.

### Dead code (Wave 9)
- **P2-133** `compare()` deprecated em password-hash.service ainda chamado em 3 lugares.
- **P2-134** Pagination constants duplicados em 4 lugares (3 mesmas, 1 divergente com DEFAULT=10).
- **P2-135** `SUPPORTED_LOCALES` definido em 3 lugares (1 com ordem inconsistente).
- **P2-136** Job BC enums duplicados (JobType, JobApplicationStatus em 2-3 schemas).
- **P2-137** `IdParamSchema` reinventado inline em 50+ route schemas.

### Misc
- **P2-138** Q12 status field — keep para i18n params (Q72), mas Q18 ainda viola em 2 lugares (onboarding `{success: false}`, webhook delivery).
- **P2-139** Spec coverage gap: 87/375 UCs sem spec.
- **P2-140** PR introduce 8 lint warnings (`noNonNullAssertion` em test files) — promover a error?
- **P2-141** Logs: 13 console.log/warn em test files — noise.
- **P2-142** `EventPublisher` em event-bus base sem `cause` field para errors.
- **P2-143** Deprecated `paginate()` aceita `pageSize` alias — remove após sweep.

---

## P3 — Backlog

(Lista compacta — detalhes nos waves.)

- **P3-144** `LEGACY_SECTION_TYPE_KEY_MAP` compatibility layer (mapping `skill_v1` → `skill_set_v1`) — verify se ainda necessário.
- **P3-145** `LEGACY_SECTION_KEYS` em import mapper.
- **P3-146** `DEFAULT_SILENT_THRESHOLD_DAYS` exported mas usado só internamente.
- **P3-147** Trivial pass-through UCs (~30) — arquiteturalmente ok, mas inventory.
- **P3-148** ts-config flags secundários: `noUnusedLocals`, `noUnusedParameters` ausentes.
- **P3-149** Bundle size: OpenAI lib import top-level OK (lazy-init), inventoriado.
- **P3-150** Fixtures factory naming: 25+ tests com `create*` em vez de `build*`.
- **P3-151** Console.log em 13 test files (noise).
- **P3-152** `FeatureFlagToggledEvent.EVENT_TYPE` (vs convencional `TYPE`) — naming inconsistente.
- **P3-153** Soft-delete extension cleanup-on-disconnect rely on observer unsubscribe.
- **P3-154** `_e2e-preseed.ts` / `_walk-bc-deps.ts` cross-link em README.
- **P3-155** `notifications` BC singular vs outras BCs plurais.
- **P3-156** Generic file names: `cleanup.helper.ts`, `export.processor.ts`.
- **P3-157** SSE InMemorySseHubAdapter cleanup pode deixar dead Subjects.
- **P3-158** Authorization `DEFAULT_CACHE_TTL_SECONDS = 60` duplicate em 2 lugares.
- **P3-159** AWS SDK type variance fix com `as any` em s3-upload — keep documentado.
- **P3-160** Public/private bucket ACL não documentado em CLAUDE.md.
- **P3-161** Anemic domain models (~60% das entities são interfaces) — design choice intencional, document.
- **P3-162** Q19 session endpoint test coverage — no new specs added with refactor.
- **P3-163** OpenAPI tags inconsistentes entre routes.
- **P3-164** `bun audit` não rodado em CI — adicionar workflow.
- **P3-165** `gitignore` redundância: `.env.docker` listado 2x.
- **P3-166** Test factories sem JSDoc explicando defaults.
- **P3-167** Email templates não compilados em build (rendered runtime each time).
- **P3-168** SCRIPTS: codemods/ contém arquivos one-off de migration sweep — mover para `scripts/migrations/` post-cutover.
- **P3-169** Some BCs sem `index.ts` (8 BCs) — feed, jobs, automation, badges, ai, integration, success-stories, platform.
- **P3-170** PostComment audit trail incomplete (P1-067 já listado, P3 follow-up: backfill timestamp para registros existentes).
- **P3-171** Test count diff PR — ~ +60 specs net (rough estimate, tooling-dependent).
- **P3-172** Session cookie CSRF defense recommendation — usar handshake.auth.token em vez de cookie em WS.
- **P3-173** Crons SkillDecay/ViewsProjection sem lock (wasteful, idempotent).
- **P3-174** Reconnect protocol não documentado para WS clients.
- **P3-175** Pagination CACHE_TTL.SHORT/MEDIUM/LONG/DAY hide semantics, use CACHE_PRESETS.
- **P3-176** Tech-skills sync `deletePattern('tech:skills:*')` over-invalidation aceitável (rare op).
- **P3-177** PostComment sem index `@@index([authorId, createdAt])` (P1-024/025 já cobre PostLike/Bookmark; P3 para PostComment).
- **P3-178** OpenAI lib import top-level — keep, lazy-init OK.
- **P3-179** N+1 em sync services (skills-data-sync, tech-niches-sync, languages-sync) — seed ops, infrequent.
- **P3-180** GitHub import N+1 em repository post creation (max 3 repos, infrequent).
- **P3-181** AutoApply worker N+1 em rage-apply loop (rate-limited to 20 per run).
- **P3-182** SkillDecayWorker N+1 em scan loop (cron, infrequent).
- **P3-183** API descriptions em Swagger inconsistentes (PR adds many, some missing).
- **P3-184** Documentation: PR description não detalha breaking changes para frontend.
- **P3-185** Route examples (`example:` em Zod schemas) inconsistente — alguns têm, outros não.
- **P3-186** Environment-aware logging: dev vs prod log levels não claramente diferenciados.
- **P3-187** No global `.editorconfig` (ou está incompleto).
- **P3-188** Some scripts mix English/Portuguese in messages.
- **P3-189** Bundler-level optimizations não exploradas (Bun has tree-shake).
- **P3-190** 2FA backup code regen flow — verificar se há rate limit.
- **P3-191** Audit log de feature flag toggle não inclui `correlation_id` (related: P3-186 tracing).
- **P3-192** `ResumeStyle` mantém PKs antigas de `ResumeTheme` para data continuity — documentar gotcha.
- **P3-193** New scoring subsystem (FitProfile, FitQuestion, FitAnswer) — sem teste integration end-to-end.
- **P3-194** Embedding vector(1536) field no Job — sem documentação de qual modelo gera (OpenAI text-embedding-ada-002?).
- **P3-195** Soft-delete `updateMany` guard adicionado em PR (Q49) — verificar coverage por test.
- **P3-196** `BatchedDocumentScoreCalculator` no Style Score — documentar como roda em prod.
- **P3-197** Feature flag dependency graph no schema — adicionar índice em FeatureFlagDependency(dependencyId, dependentId)?
- **P3-198** Default `LOG_LEVEL` em test (`error`) suprime warnings — alguns warnings importantes podem passar despercebidos.
- **P3-199** `swagger:generate` script lê todos os routes — caching incremental para CI?
- **P3-200** Composição lifecycles (`initLifecycle`) em algumas comp roots ainda não usa o helper Q35.
- **P3-201** Múltiplos enum em `prisma/schema/enums.prisma` poderiam ser organizados por BC.
- **P3-202** Role hierarchy retornado por `RoleResolverService` — testar com 100+ roles.

---

# Parte 3 — Análises temáticas (deep-dive)

## 3.1 Security

### 3.1.1 OWASP Top-10 sweep

| Vulnerability | Status | Findings |
|---|---|---|
| **A01 Broken Access Control / IDOR** | ⚠️ | P0-004 export endpoints; P0-005 delete-upload; P1-043 WS conversation handlers; P2-120 ownership inline em UCs |
| **A02 Cryptographic Failures** | ⚠️ | P0-001 JWT_SECRET default; P1-072 2FA bcrypt cost 10 |
| **A03 Injection** | ✓ | Prisma usa parameterized queries. Spot-check `$queryRaw` usage — sem string concat de input. |
| **A04 Insecure Design** | ⚠️ | P0-015 S3 public-read default; P3-160 ACL não documentada |
| **A05 Security Misconfiguration** | ⚠️ | P1-029 CORS wildcard em dev; P1-032 APP_URL fallback localhost; P0-002 WS sem origin |
| **A06 Vulnerable Components** | ⚠️ | `bun pm audit` não rodado em CI (P3-164). puppeteer/openai/nodemailer histórico de CVEs. |
| **A07 Auth Failures** | ⚠️ | P0-003 password change sem auth; P1-030 forgot-password sem rate limit |
| **A08 Software/Data Integrity** | ✓ | Atestation chain configurado em CI. |
| **A09 Logging Failures** | 🔴 | P0-007 P0-008 PII em logs |
| **A10 SSRF** | 🔴 | P0-013 link preview; P0-014 webhook delivery |

### 3.1.2 Rate limiting

Rate-limiter middleware existe (`rateLimitStage`) e está aplicado em rotas críticas (login: 5/min — confirmado em authentication.routes.ts:120). Gaps:
- `/v1/auth/forgot-password` sem rate limit (P1-030)
- WebSocket sem rate limit per-connection (P1-039)
- Pagination cap clamping não enforced explicitly em todas as routes (depends on schema parsing)

### 3.1.3 Secrets / env / config

14 callsites lendo `process.env.X` direto (bypass ConfigPort) — listados em P1-031. Defaults inseguros: P0-001 (JWT), P1-032 (APP_URL).

### 3.1.4 Input validation (Zod)

- `z.unknown()` usado intencional em 78 lugares — bem documentado (DSL content, theme AST).
- Sem `z.passthrough()` (✓).
- Schema strictness enforced em route mounter via Zod parse.

### 3.1.5 Dependency CVE scanning

Não rodado no PR. Recomendação: adicionar `bun pm audit` ao CI (workflow novo).

---

## 3.2 Performance

### 3.2.1 N+1 queries

- Skill decay service (low impact, daily cron, 5000 items × 2 queries = 10k → use upsert)
- Sync services em skills-catalog (seed ops, infrequent)
- GitHub import (max 3 repos per import, infrequent)
- AutoApply worker (rate-limited 20/run, low impact)
- Rage-apply (infrequent)

### 3.2.2 Missing Prisma indices (P1)

| Tabela | Coluna | Query | Impact |
|---|---|---|---|
| `PostLike` | `userId` | `listReactionsByUser` | Full table scan, hot path |
| `PostBookmark` | `userId` | bookmark list per user | Full table scan |
| `PostComment` | `authorId` | `listUserComments` | Full table scan |

Fix: `@@index([userId, createdAt])` em PostLike + PostBookmark; `@@index([authorId, createdAt])` em PostComment.

### 3.2.3 Hot paths sem cache

- Feed timeline (P1-028)
- Resume share by slug (P1-026 stampede)
- Job match (P1-027 stampede + AI cost)

### 3.2.4 Cache (TTL/stampede/invalidação/patterns)

Detalhes em §3.7.1.

---

## 3.3 Arquitetura (DDD/hexagonal)

### 3.3.1 Ports / adapters

- ✓ Estrutura sólida: 27 BCs, todos com domain/application/infrastructure separation.
- ✓ Zero adapters em domain/, zero ports em infrastructure/.
- ⚠️ 1 violação: `variant-repository.port.ts` em `application/ports/` em vez de `domain/ports/` (P2-104).

### 3.3.2 Use cases

- ✓ Zero UCs importam Prisma direto.
- ✓ Zero UCs importam `*.adapter.ts` direto.
- ⚠️ Q28 partial: 22 chamadas `$transaction` direto fora de `runInTransaction`.

### 3.3.3 Repositórios (Q10 violations)

47 repos com naming antigo:

**`findAll*` (deveriam ser `list*`):**
- `tech-skill.repository.ts:14` `findAllActive`
- `tech-area.repository.ts:14` `findAllActive`
- `tech-niche.repository.ts:14` `findAllActive`
- `programming-language.repository.ts:14` `findAllActive`
- `spoken-languages.repository.ts:14` `findAllActive`
- `prisma-mec-institution.repository.ts:38,101,125` `findAllActiveInstitutions`, `findAllDistinctUfs`, `findAllExistingInstitutionCodes`
- `prisma-mec-course.repository.ts:79,~90` `findAllDistinctKnowledgeAreas`, `findAllExistingCourseCodes`
- (~14 total)

**`findOne*` (inconsistente):**
- 5 admin repos em skills-catalog usam `findOne(id|slug|code)` em vez de `findById/findBySlug/findByCode`.

### 3.3.4 Domain layer

- ✓ Zero leakage de framework (Elysia, NestJS, Express, AWS, Redis, BullMQ, Axios) em domain/.
- ✓ Prisma `import type` é aceitável (Q11).
- ⚠️ `TranslationCoreService` faz `fetch()` direto — domain service não deveria (P2-103).

### 3.3.5 Anemic models

- ~60% das entities são `interface` puras (sem behavior). Design intencional per CLAUDE.md (read-model pattern).
- Entities ricas: `Role`, `Permission`, `Session`, `RankedCandidate` — protegidas com private constructors + validation.

### 3.3.6 Composition roots / DI / boot

- 27 BCs, todos com `*.composition.ts`.
- 8 BCs sem `index.ts` (P3-169): ai, automation, badges, feed, integration, jobs, platform, success-stories.
- 7 BCs com `export *` (uncurated): resumes, analytics, collaboration, career-graph, export, presentation, social.
- Platform é hub: importado por 24/27 BCs, com 211 imports diretos de PrismaService (P1-048). Falta `RepositoryPort` abstração.

### 3.3.7 Cross-BC coupling

- 405/508 (79.7%) cross-BC imports violam boundaries.
- 11 cycles de dependência (todos passando por platform).
- Top offender: `automation/run-rage-apply` importa `resume-tailor.service` direto (P1-046).

---

## 3.4 Observability

### 3.4.1 Logging (Q20/Q21/Q22)

- ✓ Q20: zero direct AppLoggerService usage fora de composition root.
- ✓ Q21: zero positional `logger.error()` calls.
- ⚠️ Q22: 1 violação (`translation.composition.ts:20-23` inline console).
- ✓ Logs com context tag em todos os call sites verificados.
- 🔴 PII: P0-007 e P0-008.

### 3.4.2 Métricas Prometheus

- 9 metrics defined.
- 4 ativos (`exportCompleted`, `scoreComputed`, `exportDuration`, `scoreComputeDuration`).
- 5 órfãs (P1-023).

### 3.4.3 Audit log

- ✓ Q50 compliant: `AuditLogPort` canonical em uso pelo identity, feature-flags.
- ✓ Q51 strict by default (throws).
- ⚠️ Q52 lenient mode subutilizado (0 callers — verificar se algum flow deveria usar).

### 3.4.4 Tracing / correlation IDs

- 🔴 NÃO implementado. Sem `X-Request-ID` middleware, sem AsyncLocalStorage, sem propagação a workers.
- Effect-batch tem `correlationId` para realtime mas não conectado ao request flow.
- Recomendação: adicionar middleware Elysia + AsyncLocalStorage + injetar em LoggerPort.

---

## 3.5 HTTP layer

### 3.5.1 Rotas em swagger

- ✓ 347 paths documentados.
- ✓ Todas têm `openapi` metadata.
- ✓ Todas têm `response` ou binary/streaming declaration.

### 3.5.2 Status codes

- ✓ Q17 implementado. 16 POSTs explicitamente declaram 200 (legítimos: action endpoints).
- ⚠️ DELETE inconsistente: alguns 204, alguns 200 com body (P1-021).

### 3.5.3 Headers/cookies

- ✓ Cookies via `ctxCookieWriter(ctx)` drained pelo mounter.
- ✓ Cache-Control via `route.headers` ou `withHeaders()`.
- ✓ Zero direct `set.cookie` em handlers.

### 3.5.4 Error handling

- ✓ Canonical error.mapper.ts trata todos os DomainException.
- ✓ Zero try/catch swallowing em route handlers (exceto health probes — intencional).
- ⚠️ DomainException sem `cause` field — P1-022.

### 3.5.5 Exceptions

- ✓ Hierarquia sólida: todos extendem DomainException ou subclass.
- ✓ Status hints declarados para 400/401/403/404/409/422/429.
- ⚠️ Exceptions internas (`ThemeCompilationError`, etc) extendem raw `Error` — risco quando re-thrown perde cause.

---

## 3.6 Workers / Jobs / Eventos / Realtime

### 3.6.1 Workers Q37 — runWithFailureMode

0/13 workers usam o helper. Failure mode é implícito via try/catch + throw (queue) ou catch + log (cron). Wire o helper em todos.

### 3.6.2 Idempotência

- ✓ Maioria via DB unique constraints + upsert.
- ✓ Documentado: `NotificationDigestWorker` (`emailDigestSentAt`), `WeeklyDigestWorker` (`UserWeeklyDigestLog`), `FitProfileExpiryReminderWorker` (Redis flag).
- ⚠️ TimeCapsuleWorker e AntiGhostingWorker carecem de dedup explícito (P0-010).

### 3.6.3 DLQ / retry

- ✓ Platform queues (export, email): `attempts: 3 + exponential backoff + 7-day removeOnFail`.
- 🔴 Outras 11 queues: `attempts: 0` default. Job falha → vai para failed state, sem retry.

### 3.6.4 Crons sem distributed lock

- 🔴 P0-010: TimeCapsuleWorker, AntiGhostingWorker.
- ⚠️ SkillDecayWorker, ViewsProjectionWorker (wasteful but safe).
- ✓ NotificationDigestWorker, WeeklyDigestWorker (protegidos via DB flag).

### 3.6.5 Eventos sem handler ou silenciosos

- 14 dead events (P1-035).
- 15 handlers sem error handling (P1-036).

### 3.6.6 WebSocket

- 🔴 Sem origin validation (P0-002, P1-044).
- 🔴 4 handlers sem schema validation (P1-038).
- 🔴 Sem rate limit per-connection (P1-039).
- 🔴 Sem max message size (P1-040).
- ⚠️ Broadcast O(N) síncrono (P1-041).
- ⚠️ Status broadcast sem debounce (P1-042).
- ⚠️ Sem reconnect/catch-up protocol (P2-100).

---

## 3.7 Cache & Storage

### 3.7.1 Cache

- ✓ Q32 100% compliant (lint em error mode, 0 violações).
- ⚠️ Q31 partial: 8 const TTL locais (6 migráveis).
- 🔴 2 stampede locations: GetShareBySlugUseCase, ComputeMatchUseCase (P1-026, P1-027).
- 🔴 Job mutations não invalidam match cache (P0-017).

### 3.7.2 Storage (S3)

- ✓ Validation: MIME + size para profile/logo upload (5MB cap).
- ⚠️ Post image upload sem magic bytes (P1-059).
- 🔴 Authz gaps: P0-004, P0-005.
- ✓ Pre-signed URL TTL: 5min (apropriado).
- ⚠️ Sem cleanup worker para orphans (P1-060).
- 🔴 ACL public-read default (P0-015).

---

## 3.8 i18n cross-código

- 🔴 P1-045: 16 routes com hardcoded EN strings.
- ⚠️ P2-111: fallback section labels EN-only.
- ⚠️ P2-112: date formatting hardcoded en-US ou pt-BR sem context.
- ⚠️ P2-113: plural rules ad-hoc, ok para en+pt-BR mas não escalável.
- ✓ Locale negotiation correto (negotiateLocale canonical).
- ✓ Resume content locale vs UI locale corretamente separados (Q27).
- ✓ Catalog parity tests passam (69/70).

---

## 3.9 TypeScript / naming

### 3.9.1 TS strictness

- 🔴 4 strict flags ausentes (P1-049).
- 🔴 `noFallthroughCasesInSwitch: false` explícito (P1-050).
- ⚠️ 211+ non-null assertions em routes (P1-052).
- ⚠️ 218 double casts (P2-089).
- ✓ Zero `@ts-ignore`.
- ✓ Zero implicit any.

### 3.9.2 Naming

- ⚠️ 17 adapters com class name fora do padrão (P2-087).
- ⚠️ 2 `.adapter.ts` files sem class (P2-088).
- ⚠️ Q67 partial: 17 routes problemas (P2-086).

---

## 3.10 Tests

### 3.10.1 Cobertura por BC

🔴 **9 BCs com 0 specs** apesar de 31+ UCs (P0-016).

### 3.10.2 Flakiness

- 13 sleeps (P1-018)
- 166 silent skips (P1-019)
- Non-deterministic generators (P1-020)

### 3.10.3 Mocks tautológicos

- 1 instância documentada em `email-flows.integration.spec.ts` (intencional, comentado).

### 3.10.4 Fixtures hardcoded

- ✓ `test/shared/factories/` bem organizado, Q57 compliant.
- ⚠️ 25+ tests legacy ainda usam `create*`/`fixture*` (P2-085).

### 3.10.5 E2E + integration impact

- ✓ 24 E2E specs concurrent-safe (per-test isolated user via `freshInDbUser`).
- ✓ 36 integration specs single-BC focus.
- ✓ 22 static-analysis specs (architecture/security/contract).

---

## 3.11 Feature flags

- ✓ DAG cycle detection robusta (FlagGraphCycleError no boot).
- ✓ Cache layer sólido (Redis + 60s TTL + in-process snapshot).
- ✓ Audit log strict por toggle.
- ✓ Lint Q32 zero violações.
- ⚠️ Sem owner/sunset fields no schema (P2-107).
- ⚠️ Hardcoded `'resumes.export.pdf'` literal em export.routes.ts:70 (P2-108).
- ⚠️ Sem suporte percent rollout (P2-109).
- 🔴 Feature-flag guard declared mas not implemented (P1-078).

---

## 3.12 Dívida técnica residual & dead code

### 3.12.1 TODOs/FIXMEs introduzidos pela PR

Spot-check: poucos TODOs novos. PR é majoritariamente cleanup.

### 3.12.2 Código legacy não removido

- `compare()` em password-hash deprecated mas usado (P2-133).
- `findAll*` repos (P2-081).
- Pagination consts duplicados (P2-134).
- Two `reset-password.use-case.ts` (P2-119).

### 3.12.3 Lint warnings

- 8 noNonNullAssertion warnings em test files (P2-140).

### 3.12.4 Migrações staged incompletas

- ✓ UUID v7 phase 1 + 2 ambos no PR.
- ✓ Cache lint warn → error: completo.

### 3.12.5 Dead code agressivo

- 14 dead `DomainEvent` (P1-035).
- 5 dead Prometheus metrics (P1-023).
- Diversos legacy maps preservados intencionalmente para compat (P3-144, P3-145).

---

## 3.13 Tooling, scripts, repo config

- Scripts inventário: 13 públicos + 2 internos + 7 codemods + 2 lib modules.
- ⚠️ Codemods não documentados (P2-124).
- 🔴 3 scripts destrutivos sem dry-run (P1-069/70/71).
- ⚠️ tsconfig 4 flags ausentes (P1-049).
- ⚠️ Biome test files com noUnusedVariables warn (P2-126).
- ⚠️ env vars não documentadas em .env.example (P2-128).
- ✓ CI workflows bem estruturados (concurrency, paths-filter, sharded tests).

---

## 3.14 DX (Developer Experience)

Mudanças notáveis introduzidas pelo PR:

- **Schema-first routes**: Zod schemas em `*.routes.schemas.ts` com bundle types — type-safe end-to-end. Onboarding mais rápido.
- **Auto-201 para POST**: dev não precisa lembrar de declarar status code.
- **Success message i18n**: dev retorna `{code, params}` e o framework renderiza.
- **Repository naming get/find/list**: contract claro — sabe se método throws ou retorna null sem ler implementação.
- **Factory naming build/freshInDb**: sabe imediatamente se o helper toca DB.
- **runInTransaction wrapper**: TX boundaries explícitas, evita `$transaction` direto.
- **shared-kernel composition primitives**: initLifecycle, registerHandler, OnShutdownPort, runWithFailureMode — deveriam reduzir boilerplate (mas Q36/Q37 não foram adotados ainda).

Pontos negativos para DX:

- Q67 partial — 7 routes sem `.routes.schemas.ts` quebram a convention "look here for the schema".
- 8 BCs sem `index.ts` — onde está o public API?
- Cycles de dependência em platform — confunde reviewers.
- 211+ `ctx.user!` assertions — força reviewer a confiar no auth middleware sem tipo.

---


# Parte 4 — Achados por bounded context

Tabela compacta — para cada BC, summary de achados taggeados por severity. BCs sem achados notáveis são listados como `✓ no findings`.

## ai

| Severity | Finding | Ref |
|---|---|---|
| P3 | OpenAI lib top-level import OK (lazy-init via assertConfigured) | P3-149 |
| P3 | Sem index.ts (public API undefined) | P3-169 |

## analytics

| Severity | Finding | Ref |
|---|---|---|
| P2 | 5 pares de events duplicados byte-by-byte (analytics + shared-kernel) | P2-105 |
| P2 | resume_analytics scoring columns dropped por scoring_refactor migration — código deve estar pre-updated | (PR migration) |
| P2 | `getIndustryBenchmarks` deprecated mas ainda usado | (Wave 9) |

## automation

| Severity | Finding | Ref |
|---|---|---|
| P1 | 4 UCs / 0 specs (test coverage) | P0-016 |
| P1 | run-rage-apply importa direto de resumes BC (cross-BC violation) | P1-046 |
| P3 | Sem index.ts | P3-169 |

## badges

| Severity | Finding | Ref |
|---|---|---|
| P1 | 6 UCs / 1 spec (low coverage ratio) | P0-016 |
| P3 | Sem index.ts | P3-169 |

## career-graph

| Severity | Finding | Ref |
|---|---|---|
| P0 | 1 UC / 0 specs | P0-016 |

## collaboration (chat + sharing + admin)

| Severity | Finding | Ref |
|---|---|---|
| P1 | 4 chat handlers sem validateWsMessage (Q39 partial) | P1-038 |
| P1 | conversation:leave + typing:stop sem isParticipant check | P1-043 |
| P1 | Conversation findOrCreate race condition | P1-054 |
| P1 | send-message-to-conversation sem TX | P1-056 |
| P2 | chat.mapper.ts em wrong layer (application/mappers) | (Wave 5) |
| P2 | Events duplicados (collaboration + shared-kernel) | P2-105 |

## dsl

| Severity | Finding | Ref |
|---|---|---|
| P2 | Theme DSL service usa Map private (in-memory cache, intencional) | (Wave 5) |
| P2 | dsl envolvido em cycle export → dsl → platform | P1-047 |

## export

| Severity | Finding | Ref |
|---|---|---|
| P0 | Endpoints PDF/JSON/LaTeX sem ownership check | P0-004 |
| P1 | Feature-flag guard declared mas not implemented | P1-078 |
| P2 | export-latex.use-case.ts 266 linhas (god UC) | P2-118 |
| P2 | Export upload não atomic — orphan files (P2-097) | P2-097 |
| P2 | Routes file 457 linhas (split candidate) | P2-091 |
| P2 | Cycle export → dsl → resumes → platform | P1-047 |
| P3 | Sem index.ts curated | P3-169 |

## feed

| Severity | Finding | Ref |
|---|---|---|
| P0 | 21 UCs / 0 specs | P0-016 |
| P0 | SSRF em fetch-link-preview adapter | P0-013 |
| P1 | Feed timeline sem cache (hot path) | P1-028 |
| P1 | Missing index PostLike(userId), PostBookmark(userId) | P1-024, P1-025 |
| P1 | Post image upload sem magic bytes validation | P1-059 |
| P2 | feed.routes.ts 473 linhas (split candidate) | P2-091 |
| P2 | Hashtag como string[] sem VO | P2-106 |
| P3 | PostComment falta deletedAt | P1-067 |

## fit-profile

| Severity | Finding | Ref |
|---|---|---|
| P0 | 12 UCs / 0 specs | P0-016 |
| P1 | submit-fit-answers UC — verify TX wrapping em adapter | P1-055 (related) |
| P3 | Subsystem novo (FitProfile/FitQuestion/FitAnswer) sem integration end-to-end | P3-193 |

## identity (auth, authorization, users, account-lifecycle, password-management, two-factor-auth, email-verification)

| Severity | Finding | Ref |
|---|---|---|
| P0 | JWT_SECRET default unsafe | P0-001 |
| P0 | password change endpoint sem auth no swagger (PR introduced) | P0-003 |
| P0 | Login response perdeu accessToken/refreshToken/expiresIn | P0-006 |
| P0 | Email PII em UserMutationRepository | P0-007 |
| P0 | authz_refactor migration drops Group sem export | P0-009 |
| P1 | Account creation sem TX (consents) | P1-055 |
| P1 | 2FA backup codes bcrypt cost 10 (vs 12 password) | P1-072 |
| P1 | forgot-password sem rate limit | P1-030 |
| P2 | Domain layer rich (Role, Permission, Session) — boa qualidade | (Wave 8) |
| P2 | Authorization circular (cycles via platform) | P1-047 |
| P3 | 2FA backup code regen rate limit | P3-190 |

## import

| Severity | Finding | Ref |
|---|---|---|
| P1 | 7 UCs / 2 specs (low ratio) | P0-016 |
| P3 | LEGACY_SECTION_KEYS map preservado | P3-145 |

## integration (mec-sync, github, upload)

| Severity | Finding | Ref |
|---|---|---|
| P0 | DELETE upload sem ownership (BUG-027) | P0-005 |
| P1 | mec-download script destrutivo sem dry-run | P1-069 |
| P3 | Sem index.ts | P3-169 |
| P3 | Sync N+1 (seed ops, infrequent) | P3-179 |

## job-match

| Severity | Finding | Ref |
|---|---|---|
| P0 | JobUpdatedEvent sem handler — match cache stale | P0-017 |
| P1 | ComputeMatchUseCase cache stampede (4 AI calls × N concurrent) | P1-027 |

## jobs

| Severity | Finding | Ref |
|---|---|---|
| P0 | 23 UCs / 0 specs | P0-016 |
| P1 | AntiGhostingWorker sem distributed lock — duplicate emails | P0-010 |
| P2 | jobs.routes.ts 573 linhas (god routes file) | P2-091 |
| P2 | Job BC enums duplicados (JobType, JobApplicationStatus) | P2-136 |
| P2 | Primitive obsession: JobTitle, applyUrl as strings | P2-106 |
| P3 | Sem index.ts | P3-169 |

## notifications

| Severity | Finding | Ref |
|---|---|---|
| P0 | 13 UCs / 0 specs (em UC dirs) | P0-016 |
| P3 | BC name singular (vs plural pattern) | P3-155 |
| P3 | NotificationDigestWorker idempotente (✓), mas sem distributed lock recommend |  |

## onboarding

| Severity | Finding | Ref |
|---|---|---|
| P1 | Q18 violation: { success: false } envelope returned | P2-138 |
| P1 | restart-onboarding sem TX | P1-057 |
| P2 | onboarding.routes.ts 528 linhas | P2-091 |

## platform (commons, audit, cache, email, feature-flags, i18n, jobs, metrics, prisma, realtime, ui-metadata, webhooks, common.health, test-runner)

| Severity | Finding | Ref |
|---|---|---|
| P0 | SSRF em webhook delivery | P0-014 |
| P0 | S3 ACL public-read (s3-upload.service) | P0-015 |
| P0 | Email PII em EmailSenderService | P0-008 |
| P1 | 5 dead Prometheus metrics | P1-023 |
| P1 | 211 imports diretos de PrismaService de outros BCs | P1-048 |
| P1 | Realtime translator errors swallow events sem DLQ | P1-075 |
| P1 | Realtime translator sem locale context | P1-074 |
| P1 | EventBus in-process only — multi-instance loses events | P2-099 |
| P1 | Feature-flag guard not implemented | P1-078 |
| P2 | 11 cycles via platform | P1-047 |
| P2 | Sem index.ts curated | P3-169 |
| P2 | Webhooks sem version field em event schemas | P2-098 |

## presentation (public-resumes, share)

| Severity | Finding | Ref |
|---|---|---|
| P1 | GetShareBySlugUseCase cache stampede | P1-026 |
| P2 | Cache TTL 60s muito curto para resume público | (Wave 5) |
| P2 | Events duplicados (presentation + shared-kernel) | P2-105 |

## recruiting

| Severity | Finding | Ref |
|---|---|---|
| P0 | 1 UC / 0 specs | P0-016 |
| P3 | RankedCandidate é entity rica (boa) | (Wave 8) |

## resume-quality

| Severity | Finding | Ref |
|---|---|---|
| P0 | 3 UCs / 0 specs | P0-016 |

## resumes (core, resume-versions, sections, section-types, time-capsule, variants)

| Severity | Finding | Ref |
|---|---|---|
| P0 | TimeCapsuleWorker sem distributed lock | P0-010 |
| P0 | drop_resume_email_contact migration risk | P0-012 |
| P1 | resume-styles refactor (ResumeTheme → ResumeStyle data preservation) — verify post-deploy | (PR migration) |
| P1 | RESUME_RELATIONS_INCLUDE sem take limit | P2-115 |
| P2 | resumes/core/resumes.routes.ts 457 linhas | P2-091 |
| P2 | variant-repository.port em wrong layer | P2-104 |
| P2 | Listings sem SELECT (full row fetch) | P2-114 |
| P3 | LEGACY_SECTION_TYPE_KEY_MAP compat layer | P3-144 |

## resume-styles

| Severity | Finding | Ref |
|---|---|---|
| P0 | 7 UCs / 0 specs | P0-016 |
| P1 | StyleScore monotônico via DB trigger — não documentado | P1-068 |
| P3 | ResumeStyle preserva PKs antigas — gotcha | P3-192 |

## skills-catalog (admin, tech-skills, spoken-languages, skills)

| Severity | Finding | Ref |
|---|---|---|
| P1 | Q10 violations concentradas aqui (8 repos com findAll*) | P1-053 |
| P2 | Inline schemas em skills.routes.ts, spoken-languages.routes.ts | P2-086 |
| P3 | Sync services com N+1 (seed ops) | P3-179 |

## social (connection, follow, activity, skill-endorsement, skill-proficiency)

| Severity | Finding | Ref |
|---|---|---|
| P1 | UserDeletedEvent ordering (cleanup-social vs cleanup-resumes) | P1-037 |
| P1 | UserFollowedEvent / ConnectionRequestedEvent dead | P1-035 |
| P2 | Skill decay service N+1 (cron, weekly) | P3-182 |
| P3 | Sem rich domain (interfaces) | (Wave 8) |

## success-stories

| Severity | Finding | Ref |
|---|---|---|
| P0 | 4 UCs / 0 specs | P0-016 |
| P3 | Sem index.ts | P3-169 |

## translation

| Severity | Finding | Ref |
|---|---|---|
| P1 | translation.composition.ts inline console.log/warn (Q22 violation) | P1-065 |
| P2 | TranslationCoreService faz fetch() direto (domain leakage) | P2-103 |

---


# Parte 5 — Recomendações

## 5.1 Bloqueadores de merge (P0) — fix BEFORE merge

Não bloquear mecanicamente toda a PR. Mas estes são must-fix antes do deploy em prod:

| # | Finding | Ação | Esforço |
|---|---|---|---|
| 1 | P0-001 JWT_SECRET default | Remover default; throw no boot | 5min |
| 2 | P0-003 password change sem auth | Auditar route descriptor; confirmar `auth: { kind: 'jwt' }` declarado; regenerar swagger | 30min |
| 3 | P0-006 login response missing tokens | Confirmar com frontend; ou regenerar swagger ou fixar contrato | 1h |
| 4 | P0-004 export ownership | Add ownership check via guard ou repository method | 2h |
| 5 | P0-005 delete-upload ownership | Validar key.split('/')[1] === userId | 30min |
| 6 | P0-007 + P0-008 PII em logs | Trocar email por userId nos 2 callsites | 30min |
| 7 | P0-009 authz_refactor migration | Dump Group/UserGroup data em staging ANTES de aplicar; verificar que app não depende mais | 4h (ops) |
| 8 | P0-010 cron sem distributed lock | Wrap TimeCapsule + AntiGhosting em DistributedLockPort.withLock() | 1h |
| 9 | P0-013 + P0-014 SSRF | Add private IP validation (link preview + webhook) | 2h |
| 10 | P0-015 S3 ACL public-read | Documentar ou mover para private | 30min docs / 1d code |
| 11 | P0-017 JobUpdatedEvent handler | Criar handler para invalidar match cache | 1h |

**Bloqueadores hard (não fazer deploy sem estes):** 1, 2, 3, 4, 5, 7, 11.

**Bloqueadores soft (corrigir esta semana):** 6, 8, 9, 10.

## 5.2 Follow-ups recomendados pós-merge (P1)

Lista priorizada por impact / effort. Cada item ≈ 1-4h.

1. Wire `runWithFailureMode` em todos os 13 workers (Q37 partial → done).
2. Wire `OnShutdownPort` em workers (Q36 partial).
3. Add `validateWsMessage` aos 4 chat handlers (Q39 partial).
4. Add isParticipant check em typing:stop e conversation:leave.
5. Add WS rate limit per-connection + max message size + origin validation (P0-002 + P1-039 + P1-040 + P1-044).
6. Migrar 16 hardcoded EN strings para SuccessMessageEnvelope (Q8 partial).
7. Add `attempts: 3 + exponential backoff` default em todas BullMQ queues.
8. Fix 14 dead DomainEvents (handler ou remoção).
9. Add try/catch + logging aos 15 handlers silenciosos.
10. Add índices Prisma: `@@index([userId, createdAt])` em PostLike, PostBookmark, PostComment.
11. Cache stampede fix: `cache.getOrSet()` em GetShareBySlugUseCase + ComputeMatchUseCase.
12. Add cache invalidation handler para JobUpdatedEvent + JobDeletedEvent.
13. Fix 5 dead Prometheus metrics (wire ou remove).
14. Habilitar `noUncheckedIndexedAccess` no tsconfig (vai expor 211+ assertions a corrigir).
15. Add cleanup worker para orphan blobs no S3.
16. SSRF defense em link preview e webhook delivery.
17. CORS: explicit `origin: false` quando vazio.
18. Rate limit em `/v1/auth/forgot-password`.
19. APP_URL fail-fast em prod.
20. Move 14 callsites `process.env.X` direto → ConfigPort.

## 5.3 Smoke tests sugeridos pré-deploy

(Reproduz seção do executive summary com mais detalhe.)

### Smoke 1 — Auth golden path

```bash
# 1. Signup
curl -X POST http://staging/api/v1/accounts -d '{ email, password, ... }'
# 2. Verify email (manual: pegar token do email)
# 3. Login
curl -X POST http://staging/api/v1/auth/login -d '{ email, password }'
# Esperado: { accessToken, refreshToken, expiresIn, userId } OU set-cookie + { userId }
# 4. Refresh
curl -X POST http://staging/api/v1/auth/refresh -d '{ refreshToken }'
# 5. Logout
curl -X POST http://staging/api/v1/auth/logout
# 6. Password change (autenticado)
curl -X POST http://staging/api/v1/me/password/change -d '{ ... }' -H 'Authorization: Bearer ...'
# CRÍTICO: deve retornar 401 sem auth — confirmar P0-003 fixed
```

### Smoke 2 — Resume CRUD em pt-BR

Setar `Accept-Language: pt-BR` em todos os requests. Criar resume → editar → deletar. Verificar que mensagens de sucesso vêm em pt-BR (vai expor P1-045).

### Smoke 3 — Export cross-user (P0-004 test)

```bash
# Como user A:
TOKEN_A=$(login userA)
RESUMES_A=$(curl -H "Authorization: Bearer $TOKEN_A" http://staging/api/v1/resumes)
RESUME_ID_OF_USER_B="<known-id>"
# Tentar exportar resume de User B com token de User A:
curl -H "Authorization: Bearer $TOKEN_A" http://staging/api/v1/export/$RESUME_ID_OF_USER_B/json
# Esperado: 403 Forbidden (depois do fix). Hoje: 200 com PDF de User B.
```

### Smoke 4 — Match recompute cache invalidation

1. Trigger ComputeMatch para User A vs Job X → cache populated.
2. Admin edita Job X (atualiza requirements).
3. ComputeMatch novamente → deve retornar score recalculado, não cached.
4. Confirma P0-017 fixed.

### Smoke 5 — Migrations em staging clone

1. Clonar staging DB.
2. Aplicar `20260503120000_install_pg_uuidv7` → confirmar extensão criada.
3. Aplicar `20260503130000_swap_id_defaults_to_uuidv7` → criar 1 row de Resume → confirmar UUID v7 (timestamp prefix).
4. Aplicar `20260430040810_authz_refactor` → confirmar Group tables dropadas, AccessModifier criado.
5. Smoke API: signup + login + create resume — sem regressões.

### Smoke 6 — WebSocket stress

```bash
# Abrir 1 WS connection autenticada
# Enviar 100 typing:start events em 1s
# Confirmar que server NÃO derruba conexão (sem rate limit hoje, P1-039)
# Mas confirmar que server NÃO crasha
```

### Smoke 7 — Feed timeline concurrency

```bash
# 50 requests concorrentes para /v1/feed/timeline com 50 users diferentes
# Medir p50, p95, p99 latency
# Identificar bottleneck (sem cache, P1-028)
```

### Smoke 8 — i18n swagger contract

Validar com codegen do frontend que swagger atual ainda é parseável e gera tipos corretos. Confirmar que `components.schemas` vazio não quebra tooling.

---

# Apêndices

## A. Comandos úteis para reproduzir achados

```bash
# Q-conformance: contar findAll* em repos
grep -rn "findAll" src/ --include="*.ts"

# Q-conformance: contar inline z.string().datetime()
grep -rn "z\.string()\.datetime()" src/ --include="*.ts" | wc -l

# Q-conformance: console.* fora de allowlist
grep -rn "console\.\(log\|warn\|error\|info\|debug\)" src/ --include="*.ts" | grep -v "console-logger.adapter.ts" | grep -v "logger.service.ts"

# Cache lint
bun run scripts/lint-cache-direct-delete.ts

# Architecture parity
bun test ./test/static-analysis/architecture

# Contract tests
bun test ./test/static-analysis/contracts

# Find god files
find src -name "*.ts" -not -path "*/node_modules/*" -exec wc -l {} \; | sort -rn | head -30

# Dependency cycle scan (manual via grep)
for bc in $(ls src/bounded-contexts/); do
  echo "=== $bc imports ==="
  grep -rn "from '@/bounded-contexts/" src/bounded-contexts/$bc --include="*.ts" | head -5
done

# Verificar ordem dos commits do PR
git log --oneline 838b1657..refactor/backend --no-merges | head -50

# Diff de bun.lock
git diff 838b1657..refactor/backend -- bun.lock | head -50

# Cross-ref do .env.example com process.env reads
grep -rn "process.env\." src/ --include="*.ts" | grep -v test/ | grep -v config-adapter | sort -u
```

## B. Arquivos críticos referenciados

- `docs/audits/duplication-audit-2026-05-03.md` — base Q1-Q75
- `CLAUDE.md` — convenções vigentes
- `prisma/schema/*.prisma` (33 arquivos)
- `prisma/migrations/2026*` (9 migrations do PR)
- `client-swagger.json` (gerado, mas commitado para diff visibility)
- `packages/i18n/src/{errors,success-messages,index,enums,notifications}.ts`
- `src/shared-kernel/index.ts` — surface do kernel
- `src/infrastructure/elysia-adapter/elysia-bootstrap.ts` (composition root principal)
- `src/infrastructure/elysia-adapter/elysia-route-mounter.ts` (HTTP framework)
- `src/infrastructure/elysia-adapter/error.mapper.ts` (canonical error handling)
- `src/infrastructure/elysia-adapter/elysia-websocket.adapter.ts` (WS layer)
- `bun.lock` — diff de deps
- `biome.json`, `tsconfig.json`, `.github/workflows/*.yml`

## C. Limitações desta auditoria

1. **Análise estática apenas** — não simula runtime, não detecta race conditions reais, não mede latência real. Recomenda-se complementar com APM (datadog, sentry) em prod.
2. **3-way diff por arquivo não foi exhaustivo** — para os ~580 arquivos onde merge usou `-X ours`, adições do homolog podem ter sido descartadas. Inventário focado em PR #213 hardening fixes recomendado.
3. **Subagents podem ter falsos positivos** — especialmente no dead code aggressive sweep (alguns "unused exports" podem ser runtime-loaded via DI ou loaded lazily). Lista marcada com confidence (HIGH/MEDIUM/LOW) onde aplicável.
4. **Métricas de tempo / latência** — não medidas; baseadas em estimativas de complexidade.
5. **Frontend impact** — mencionado mas não inspecionado (escopo é só profile-services).
6. **CVE scan** — não rodado (`bun pm audit` deve ser feito manualmente ou via CI).

## D. Glossário

- **BC** = Bounded Context
- **DTO** = Data Transfer Object
- **VO** = Value Object
- **UC** = Use Case
- **TX** = Transaction
- **DLQ** = Dead-Letter Queue
- **WS** = WebSocket
- **SSRF** = Server-Side Request Forgery
- **IDOR** = Insecure Direct Object Reference
- **PII** = Personally Identifiable Information
- **TTL** = Time To Live
- **Q[NN]** = Decisão N da auditoria de duplicação 2026-05-03

---

**Fim do documento.** Total: ~300 findings categorizados. Tempo estimado de execução total deste audit: ~10h (10 waves × ~30-45min de subagent work, paralelizável). Próxima auditoria sugerida: 6 meses após o merge desta PR.
