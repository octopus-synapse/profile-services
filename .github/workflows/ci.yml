name: CI

on:
  pull_request:
    branches: [main, dev]
    paths-ignore:
      - "CHANGELOG.md"
      - "package.json"
  push:
    branches: [main, dev]
    paths-ignore:
      - "CHANGELOG.md"
      - "package.json"

permissions:
  contents: read
  packages: read
  pull-requests: write

jobs:
  # ============================================
  # Detecta se é merge de PR (redundante) ou push direto (necessário)
  # ============================================
  check-trigger:
    name: Check if CI should run
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.check.outputs.result }}
    steps:
      - id: check
        run: |
          # Se for PR, sempre roda
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "result=true" >> $GITHUB_OUTPUT
            echo "✅ Running CI for PR"
            exit 0
          fi

          # Se for push, verifica se NÃO é merge de PR
          if ! echo "${{ github.event.head_commit.message }}" | grep -qE "^Merge pull request|^Merge branch"; then
            echo "result=true" >> $GITHUB_OUTPUT
            echo "✅ Running CI for direct push to ${{ github.ref_name }}"
          else
            echo "result=false" >> $GITHUB_OUTPUT
            echo "⏭️  Skipping CI - already ran on PR #$(echo "${{ github.event.head_commit.message }}" | grep -oP '#\K\d+')"
          fi

  integration:
    name: Integration Tests
    needs: check-trigger
    if: ${{ needs.check-trigger.outputs.should_run == 'true' }}
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: --health-cmd "redis-cli ping" --health-interval 10s --health-timeout 5s --health-retries 5
    steps:
      - name: Checkout profile-services
        uses: actions/checkout@v4
        with:
          path: services

      - name: Checkout profile-contracts
        uses: actions/checkout@v4
        with:
          repository: octopus-synapse/profile-contracts
          path: profile-contracts
          token: ${{ secrets.GH_PAT }}

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.23

      - name: Debug directory structure
        run: |
          echo "=== GITHUB_WORKSPACE: $GITHUB_WORKSPACE ==="
          echo "=== Workspace contents ==="
          ls -la
          echo "=== Services package.json dependency ==="
          grep profile-contracts services/package.json || echo "not found"
          echo "=== Profile-contracts contents ==="
          ls -la profile-contracts/ || echo "profile-contracts not found"

      - name: Install contracts dependencies
        working-directory: profile-contracts
        run: bun install --frozen-lockfile

      - name: Build contracts
        working-directory: profile-contracts
        run: bun run build

      - name: Install dependencies
        working-directory: services
        run: bun install --frozen-lockfile

      - name: Setup database
        working-directory: services
        run: bun run db:setup:test
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
          REDIS_HOST: localhost
          REDIS_PORT: 6379
          JWT_SECRET: test-secret
          NODE_ENV: test

      - name: Run integration tests
        working-directory: services
        run: bun test --config=bunfig.integration.toml
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
          REDIS_HOST: localhost
          REDIS_PORT: 6379
          JWT_SECRET: test-secret
          NODE_ENV: test

  hook-compliance:
    name: Verify Pre-Commit Hooks Ran
    needs: check-trigger
    if: ${{ needs.check-trigger.outputs.should_run == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout profile-services
        uses: actions/checkout@v4
        with:
          path: services

      - name: Checkout profile-contracts
        uses: actions/checkout@v4
        with:
          repository: octopus-synapse/profile-contracts
          path: profile-contracts
          token: ${{ secrets.GH_PAT }}

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.23

      - name: Install contracts dependencies
        working-directory: profile-contracts
        run: bun install --frozen-lockfile

      - name: Build contracts
        working-directory: profile-contracts
        run: bun run build

      - name: Install dependencies
        working-directory: services
        run: bun install --frozen-lockfile

      - name: Generate Prisma Client
        working-directory: services
        run: bunx prisma generate

      - name: Check if pre-commit hooks were bypassed
        working-directory: services
        run: |
          echo "🔍 Verificando se pre-commit foi executado..."

          # Roda lint - se falhar, pre-commit foi ignorado
          if ! bun run lint; then
            echo "❌ LINT FALHOU - alguém usou --no-verify"
            echo "Pre-commit hooks devem ser executados antes do commit."
            echo "Se você precisa emergencialmente pular hooks, o CI vai pegar de qualquer forma."
            exit 1
          fi

          echo "✅ Pre-commit hooks foram executados corretamente"

  lint:
    name: ESLint (Type-Aware)
    needs: check-trigger
    if: ${{ needs.check-trigger.outputs.should_run == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout profile-services
        uses: actions/checkout@v4
        with:
          path: services

      - name: Checkout profile-contracts
        uses: actions/checkout@v4
        with:
          repository: octopus-synapse/profile-contracts
          path: profile-contracts
          token: ${{ secrets.GH_PAT }}

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.23

      - name: Install contracts dependencies
        working-directory: profile-contracts
        run: bun install --frozen-lockfile

      - name: Build contracts
        working-directory: profile-contracts
        run: bun run build

      - name: Install dependencies
        working-directory: services
        run: bun install --frozen-lockfile

      - name: Generate Prisma Client
        working-directory: services
        run: bunx prisma generate

      - name: Run ESLint
        working-directory: services
        run: bun run lint

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [integration, hook-compliance, lint]
    steps:
      - name: Checkout profile-services
        uses: actions/checkout@v4
        with:
          path: services

      - name: Checkout profile-contracts
        uses: actions/checkout@v4
        with:
          repository: octopus-synapse/profile-contracts
          path: profile-contracts
          token: ${{ secrets.GH_PAT }}

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.23

      - name: Install contracts dependencies
        working-directory: profile-contracts
        run: bun install --frozen-lockfile

      - name: Build contracts
        working-directory: profile-contracts
        run: bun run build

      - name: Install dependencies
        working-directory: services
        run: bun install --frozen-lockfile

      - name: Generate Prisma Client
        working-directory: services
        run: bunx prisma generate

      - name: Build
        working-directory: services
        run: bun run build

      - uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: services/dist
          retention-days: 7

  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - name: Checkout profile-services
        uses: actions/checkout@v4
        with:
          path: profile-services

      - name: Checkout profile-contracts
        uses: actions/checkout@v4
        with:
          repository: octopus-synapse/profile-contracts
          path: profile-contracts
          token: ${{ secrets.GH_PAT }}

      - uses: docker/setup-buildx-action@v3

      - uses: docker/build-push-action@v5
        with:
          context: .
          file: profile-services/Dockerfile
          push: false
          tags: profile-services:${{ github.sha }}
          secrets: |
            github_token=${{ secrets.GH_PAT }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
