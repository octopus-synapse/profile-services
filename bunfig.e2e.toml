[test]
# E2E configuration. Suites live under `test/infrastructure/e2e/`,
# wired through the `TestApp` harness in `shared/`.
root = "./test/infrastructure/e2e"
timeout = 60000

[install.scopes]
"@octopus-synapse" = { token = "$GITHUB_TOKEN", url = "https://npm.pkg.github.com" }
