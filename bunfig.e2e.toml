[test]
# E2E Test configuration for Bun
root = "./test/e2e/journeys"

# E2E tests require longer timeouts
timeout = 60000

[install.scopes]
"@octopus-synapse" = { token = "$GITHUB_TOKEN", url = "https://npm.pkg.github.com" }
