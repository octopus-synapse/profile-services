// Controllers folder kept for the DTO/Zod schemas the route descriptors
// import (`change-password.dto.ts`, `reset-password.dto.ts`). The legacy
// Nest controllers were retired; route synthesis lives in
// `../password-management.routes.ts`.
export {};
