// Translation BC infrastructure barrel — currently empty. Routes are
// declared in `translation.routes.ts` and synthesized by the Nest
// adapter; HTTP DTOs live alongside the route descriptors as Zod
// schemas, so no Nest controllers remain.
export {};
