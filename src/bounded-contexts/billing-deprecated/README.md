# Billing deprecated

Frozen snapshot of the pre-ADR-001 billing implementation. It is retained only
for migration archaeology and must not be imported, built, tested, or changed.
The active bounded context is `src/bounded-contexts/billing`.
