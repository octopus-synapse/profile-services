/**
 * Error Messages Constants
 *
 * Re-exported from the shared-kernel catalog — this file used to hold a
 * near-identical copy that had already drifted (missing
 * `ONBOARDING_ALREADY_COMPLETED`). One catalog, one place to edit.
 *
 * NOTE: these are internal English labels (log/exception `message`). The
 * user-facing text comes from `@packages/i18n` (`ERROR_DICTIONARY` /
 * `VALIDATION_DICTIONARY`) keyed by `code`, never from here.
 */
export { ERROR_MESSAGES } from '@/shared-kernel/constants/error-messages.const';
