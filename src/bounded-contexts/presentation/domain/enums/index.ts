/**
 * Presentation Domain Enums
 *
 * (Currently empty — Theme enums were removed during the scoring
 * refactor; ResumeStyle owns the visual taxonomy now. Re-add exports
 * here only when a real presentation-layer enum lands.)
 */
export {};
