/**
 * Presentation Domain Enums (shared-kernel mirror)
 *
 * Same story as `presentation/domain/enums/index.ts` — Theme enums
 * dropped during the scoring refactor. Empty re-export keeps the
 * module's `*` re-exports valid for any deep imports that still
 * reach in.
 */
export {};
