#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🧪 Pre-push checks..."

# Unit tests already run on pre-commit
# Just verify one more time with coverage info
echo "  → Running unit tests with coverage..."
bun test --coverage || exit 1

echo "✅ Pre-push passed!"
echo "💡 CI will run integration tests with real DB"
