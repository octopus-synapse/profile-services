#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Pre-commit checks (< 10s)..."

# 1. Lint (code style)
echo "  → Linting..."
npm run lint --silent || exit 1

# 2. Type check (TypeScript syntax)
echo "  → Type checking..."
npm run typecheck || exit 1

# 3. Critical tests (core business logic)
echo "  → Running 10 critical tests..."
npm run test:critical || exit 1

echo "✅ Pre-commit passed!"
echo "💡 Full test suite (710 tests) will run on pre-push"
